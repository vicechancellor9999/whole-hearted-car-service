export default function DashboardPage() {
  return (
    <section className="workspace-card" aria-labelledby="dashboard-title">
      <p className="eyebrow">正式数据工作台</p>
      <h1 id="dashboard-title">工作台</h1>
      <p>账号与权限基础已连入后端。下一步从这里进入账号管理和正式业务数据。</p>
    </section>
  );
}
