# 核心工作区连续性 — 2026-09-04

范围：业务单详情、关联检查入口、检查报告返回上下文。保留 WH 品牌与当前业务数据；本轮不改变金额、绩效、删除或交单规则。

1. 先用组件测试覆盖关联检查的创建、加载、失败重试、权限、分页，以及收费名称/描述/金额标签与分类净额。
2. 业务单以车辆身份和费用承担方为主要上下文，单号与状态保留；明细使用容器响应布局，左右区独立滚动。
3. 检查创建入口不依赖维修轮次状态；创建成功打开新报告，报告各工作区均保留来源业务单入口。读取失败不得伪装为零条。
4. 两种详情的标签在窄屏横向排列，避免占满首屏。
5. 组件测试、现有定向回归、类型检查、构建；部署 3220，实页验证桌面、窄屏、空状态、打开/取消创建与返回路径。不得为验收新建/删除真实业务数据。

## 实施与验收

- 已实现：车辆/费用承担方头部；收费分类组件（名称、描述、数量/单位、含税单价、折扣、小计）；汇总前置；横向标签栏。
- 相关检查成为独立右栏卡片，不再局限于维修中状态。支持分页、真实总数、加载/失败重试；创建成功传回报告并导航详情。检查详情头部保留来源业务单链接。
- 测试入口：`pnpm --dir apps/web test:workspace`（9 项渲染/交互测试）。旧的源码结构测试同步调整到提取后的组件，未移除业务断言。
- 3220 生产构建并重启已有服务；2026-09-04 实页：1600×900、1600×1200、390×844。浏览器有缩放，以上以 `innerWidth/innerHeight` 实测值为准。
- 1600×900：主区域 height/scrollHeight 均 900；左右区分别独立滚动。高屏对应值均 1200。相关检查与收付款相距 11.996px（12px CSS）。展开收付款历史左侧 scrollTop 保持 0。
- 390px：document.scrollWidth = innerWidth = 390；收费行无横向溢出，标签与金额完整；发现并修正头部被右侧按钮挤窄的问题。
- 已交单状态成功打开新建检查弹窗，带入正确车辆；取消正常。业务单单据标签与检查正式报告标签可打开，浏览器 error/warn 日志为空。
- 未为验收写入、修改或删除真实业务记录。创建成功回调、保留来源编号、失败后保留输入由组件测试验证；现有唯一检查报告为独立检查，因此没有在真实数据里执行来源报告返回的端到端创建链路。
- 这批仅覆盖上述核心工作区，不表示 AI、迁移、全系统交互重构已完成。
