# Whole Hearted 正式系统续接入口

## 2026-09-06 GitHub 程序员审查检查点

- 用户已要求同步当前成果供程序员审查；本轮整理现有源码、迁移、测试与交接材料，保留开发历史。
- 主要审查分支为 `codex/ai-service-wip-20260827`，从仓库 [README](../README.md) 进入。
- 本次最新结果：前端 29 文件 304 项、定向后端 6 文件 93 项通过；Web 类型检查通过，根目录类型检查存在错误。完整命令、错误位置和评审边界见 [GitHub 检查点](qa/2026-09-06-github-review-checkpoint.md)。
- 产品的剩余缺口继续按 09-05 审查入口与收敛审计处理。下方保留历史记录，其未提交状态和运行 PID 代表当时快照。

## P0-A 首批核心路径盘点 · 09-05 自动续接

- 已在 [程序员审查入口](qa/2026-09-05-programmer-review-entrypoint.md) 增补三条路径的正式/旧入口调用链和 R01–R08 风险，属于上线计划第一包的部分成果，不是全系统审查或代码修复完成。
- 优先证据：检查沟通写入未应用读取函数返回的维修工班组范围；短信外部发送与记录结果缺少独立权限/幂等闭环；客户任何非空回复被写成confirmed；检查新建没有服务端请求身份防重；部分读取/单据/保存仍无期限；旧打印及工作台/车辆入口未统一合同。详见该文档精确路径、限定结论和待补回归场景。
- 本轮未执行测试/构建、未改产品代码或真实数据、未启动服务或发送消息。实页仍/login，监听PID92917 cwd候选apps/web。get_goal返回null，未尝试重复设置目标；旧自动任务不能视为全系统上线计划已经批准实施。
- 下一实施包按上表优先级由工程负责人审查和拆解；恢复实页验收后补三端证据，不重复改已验收打印，不以文档盘点代替修复。

## 当前工作方向：09-05 上线总计划（规划交付，尚未授权全量实施）

- 用户要求完整上线计划并明确需要独立程序员审查；本次交付 [上线实施总计划](superpowers/plans/2026-09-05-production-launch-master-plan.md)。它覆盖基线审查、业务/三端交互、工程底座、完整业务、Lark迁移、云端发布、试运行、切换和回退。
- 首批工作包为 P0-A/P0-B/P1-A/P1-B。既有用户业务要求直接沿用；历史未批准规则与当前要求的分歧集中评审，不把旧基线恢复成权威。
- 本次只读核查源码和文档，新增文字计划及本入口；没有产品代码更改、数据库写入、启动/重启服务、付费调用、真实导入或新建运行副本。计划估算不是上线承诺，AI复核不是独立人类审查。
- 后续实施以用户对计划的方向及每阶段放行条件为准，不自动回到无限小补丁循环；下方保留原阶段证据，历史未完成不因计划交付而变为完成。

## 最新候选检查点：09-05 15:10（优先于下方历史检查点）

- 前轮完成现状测量与收敛审计，本轮完成程序员审查入口；不是新产品修复。先读 [审查入口](qa/2026-09-05-programmer-review-entrypoint.md) 与 [未完成项审计](qa/2026-09-05-core-paths-closure-audit.md)，不要用累计测试数量代表可部署。
- 重新检查全部真实标签，3220仍为登录页，另两页为Lark；监听PID92917、cwd候选apps/web。本次登录阻断已连续至少三个目标轮次，隔离测试与安全只读核对已执行，没有在途测试任务。最终实页验收需要用户正常登录；不得伪造会话。
- 当前范围剩余业务缺口需要解决整单折扣规范冲突及正式单据/客户沟通作废规则，不擅自裁定，不改真实数据。工作未完成；尝试登记blocked时目标工具返回“this thread has no goal”，因此没有成功改变目标状态，也未重新创建目标。恢复后按收敛审计顺序继续，不再重复已验证修复。
- 用户新增交付要求：必须能供程序员审查和接手。审查入口记录当前源码规模、HEAD、已跟踪差异与未跟踪构建目录、模块审查顺序和测试命令。没有把建议的结构重构写成已经实施，也没有全量暂存、清理或提交混合改动。

## 最新候选检查点：09-05 15:03（优先于下方历史检查点）

- 上轮PROGRESS，本轮PROGRESS。真实3220仍为/login（连续第二目标轮出现同一登录阻断）；没有绕过会话或查找真实凭据。使用外置QA/editor-harness搭隔离Vite页面，直接导入真实检查组件/样式，虚构fixture，所有写请求与其他fetch返回403，无后端代理，不是3220生产验收。
- 隔离浏览器发现新窄容器缺陷：698×931时视口sm断点强制6列，删除按钮right658超出报价卡片right639。既有verify-inspection-editor-targets红测确认两处contained=false；改inspection-editor.module.css报价容器查询680px，窄时2列、宽时6列，删除按钮对齐。正式组件只挂容器/数字区class，无业务规则变化。
- 隔离绿测698×931、391×843、768×1024、1600×900，各39控件达到44px/未裁切/页面无横溢。前端新增第三项→52控件通过→删除刚新增项→两项恢复，总额15800不变。截图93为发现时视口（按钮当时在可见范围之外，不充当目视裁切证据）；94手机、95原生修复结果已目视。DOM边界是红测证据。控制台error/warn为空。
- 范围eslint、28文件279组件、生产build/tsc、diff通过；受控部署PID92917，cwd候选apps/web确认。未真实报告保存、财务、绩效、删除、客户通知或付费AI；未重新计算24表哈希。真实登录后的触控/容器绿测仍缺失，不能由隔离页替代。
- 测试启动说明在QA/editor-harness/README.md。隔离服务session97958已主动终止exit130，临时浏览器页已关闭，viewport reset；没有待轮询进程。不要把该服务当持续运行任务。
- 下一步收敛三条路径完成度矩阵：核对当前需求/已修证据/确实未处理缺陷，优先可独立完成的检查；真实登录恢复前保持实页验收未完成。登录阻断本轮仍有实际可做工作，未达真正无进展困境，不标blocked/complete，不新建目标。

## 最新候选检查点：09-05 14:53（优先于下方历史检查点）

- 本轮 PROGRESS：整体实页巡检发现收费卡片在 660px 容器即强制名称/4数字并排，金额列约95px，JMD与数字两行。先新增外置QA脚本 verify-charge-money-lines.mjs，真实DOM Range行数红测；修 business-charge-section.module.css：金额nowrap，容器560px起4列、880px起名称与数值并排，数值列保留内容宽度。
- PID92237实页绿测：原生1337×1050、1600×900、768×1024、391×843，15个金额均1行且在列内，页面无横溢。右栏展开历史后独立overflow:auto，clientHeight763/scrollHeight1395，区块间距11.998px。截图90为原生修复后；91平板、92手机客户联150%，已目视。viewport截图额外画布不当成应用空白。
- 双联只读复核：CUS-20260905-0002 V21/R1仍连续2页，办公室/客户跳转、中文/英文文件href切换正常。手机150%下画布892px、阅读区344/scroll893、页面391，无外部撑宽。iPad/桌面翻页正常。未生成新正式单据，未点击下载或打印。
- 第二个实页发现：检查报价手机删除按钮约15px高、分类32px、常用编辑动作36px。verify-inspection-editor-targets.mjs在平板红测确认多个小于44px。新增inspection-editor.module.css并挂到检查与报价工作区，按钮/非radio输入至少44px、textarea80px、删除图标居中；不改内容或保存逻辑。
- 最终范围eslint、28文件279组件、生产build/tsc、diff通过，受控部署PID92512且cwd候选apps/web确认。随后实页进入/login，登录上下文已不可用（原因未查明，不猜测过期或人为退出），error/warn为空，视口已reset。第二处触控修复尚未实页绿测；不能将第一处绿测移用于它。无真实财务/绩效/删除/报告保存/通知/付费AI写入，未重做24表哈希。
- 下一步：在隔离前端测试环境验证触控修复/窄容器，不绕过真实登录；真实会话恢复后再运行两份QA脚本并复核三个尺寸。其余已确认规则冲突与下载目的地限制保持。此为首次遇到当前登录阻断，本轮仍有有效进展，目标active，不标全范围完成、不重复新建目标。

## 最新候选检查点：09-05 14:43（优先于下方历史检查点）

- 上轮PROGRESS；本轮PROGRESS。轮次删除提交前保存按账号/业务单隔离的原请求、原轮次ID/编号、原因与版本；sessionStorage当前标签页24小时，刷新pending转待核对，不自动POST。即使当前轮次已变化，仍显示原请求恢复入口，不把重试改成删除新轮次。只暂存请求提示，不作为权限或删除事实。
- 新round-deletion-storage.ts校验结构/身份/时间/体积；不能用不同payload覆盖原请求，晚回包不复活已移除提示。新RoundDeletionAttemptView呈现原对象/原因/请求号，人工原请求重试、重新读取服务器历史、关闭、核对后移除。暂存读取失败有重读；写入/清理失败保持提醒并可重试。
- 初始刷新恢复红测缺入口；错误回执红测发现仅cancelled=true而原轮次仍在时会误报成功，补原ID与工作区验证。读历史红测发现直接打开旧快照，改先GET最新维修记录，15秒现有期限，失败留在恢复卡；关闭/账号切换忽略旧读结果。旧账号迟到删除结果仅更新原账号原请求，不改新账号UI。
- 28文件279组件、范围eslint、生产build/tsc、diff通过。新测试含跨刷新当前轮变化、账号迟到回包、错轮次/原轮仍在、暂存清理失败、过期/坏数据、最新历史失败。故障与删除执行全为隔离组件/网络替身，未对真实记录提交删除。
- 最终受控上线3220 PID91862，cwd候选apps/web确认；最终实页预检/重新检查/关闭正常，error/warn为空。三尺寸正常预检391×844（窗口358.67×812，动作44px）、768×1024、1600×900（窗口576×792）无页面横溢；最后reset视口。本轮没有伪造真实暂存来展示故障卡，也没有故障卡的真实浏览器截图，不冒称全部恢复UI实页目视已验收。无真实收费、收付款、绩效、报告创建、客户通知或付费AI修改；未重做24表哈希。
- 下一步回到三条路径统一视觉验收与收敛矩阵，勿重复做已修的轮次刷新恢复。当前恢复只有本标签24小时；核对历史不是只读删除回执查询，后端虽保存repair_round回执，尚无专用只读结果消费入口，不称已具备。正式单据/沟通事实作废规则与整单折扣冲突仍不自行裁定，下载外置目的地未验证前不落盘。目标active，全范围完成未获证明。

## 最新候选检查点：09-05 14:29（优先于下方历史检查点）

- 本轮 PROGRESS：只读实页核对R3删除依赖、重新检查、交单历史及正式单据导航。391×844、768×1024、1600×900无页面横溢，手机动作均44px；截图88已落外置QA并目视，视口已reset。现有正式单据阻断未解除，未执行任何真实删除。
- 找到与共享检查结果删除不同的遗漏：delete_invalid_after_sales提交没有等待上限。两个API红测在30秒仍pending；补30秒期限覆盖头/JSON正文，超时提示结果未确认，可原窗口原请求重试或关闭核对，迟到结果不假报成功、不自动重发。正式维修记录读取原已有15秒期限，不重复修改。
- 增加真实BO组件+真实API适配器测试，只有网络替身：超时释放按钮、保留原因/确认编号、人工重试payload与requestId完全一致、可关闭。首次断言早于微任务发出请求，补flush；首次build因测试库不支持exact选项失败，改名称正则后build/tsc通过。
- 全组件27文件268项通过；最终两个相关文件59项重跑通过；范围eslint、生产build、git diff --check通过。受控部署3220 PID90914并确认cwd候选apps/web，更新后实页只读复核/关闭正常、error/warn为空。未改后台或真实收费/收付款/绩效/检查/通知；不是实际删除成功的验收。
- 已合并验收矩阵里的陈旧待办：长双联、绩效恢复、检查创建和共享删除恢复不再反复列为未做。历史检查点保留，06:32顺序明确归档。
- 真正下一步：维修轮次专用删除窗口仍仅本页保留请求，关闭/刷新会丢原请求身份；与已完善的共享RecordDelete恢复不是同一路径，需隔离测试评估安全恢复。随后统一三路径视觉证据。正式单据/沟通事实的作废规则及整单折扣资料冲突不自行裁定；下载目标未确认外置前不点击落盘。目标active，不另建目标/自动任务。

## 最新候选检查点：09-05 14:19（优先于下方旧检查点）

- 本轮 PROGRESS：真实手机391×844两次复现 ChoiceCards 原生radio点击误选相邻语言。DOM确认 sr-only 控件只有1×1px，y比所属48px高卡片偏后64.33px，三个控件都在所属label之外；不是语言值存储或PDF缓存问题。可见label中心命中正常。未将一次工具超时直接当产品bug。
- 修复 shared/choice-cards.tsx：label relative，原生input absolute/inset-0/完整宽高/opacity-0，保持native radio、disabled、required、name/value、默认值、peer checked/focus样式。hover改由卡片group触发。无新状态库、无业务规则或打印实现改动。
- 实页回归脚本：QA目录 verify-choice-card-targets.mjs，通过现有Browser运行，只做语言UI切换与DOM几何断言，不发业务写入。旧PID87721明确红测（三个inside=false）；新PID90254下391×844、768×1024、1600×900全部inside=true，目标85.33×48，zh→en→bilingual→en→zh逐项通过。英文生成EN.pdf，切BI立即撤旧下载，再生成BI.pdf 1页；日志error/warn为空。
- 27文件265组件、范围eslint、生产build（含tsc）、diff通过。3220受控更新PID90254，cwd候选apps/web验证；没有向3210/正式工作树写入。此CSS布局回归用真实Browser而不是jsdom伪造布局。
- 单据只读复查：现有 CUS-20260905-0002 V21/R1，办公室/客户联连续2页。1600×900与768×1024跳联，391×844在150%下页面scrollWidth=391，单据内scrollWidth893/clientWidth342，工具可达，改回fit。未生成新业务单版本。此前新增版本/快照与页面已生成提示一致，但不归因操作者、不回滚、不重置上午数据基线。
- 截图恢复：本轮实际看到桌面双联与手机放大截图；原生尺寸下检查报告截图已存 87-language-target-after.jpg 并目视。viewport覆盖截图带额外空白画布，不能把它等同应用布局空白；DOM实测与原生截图分开记录。已reset视口。
- 限制/下一步：真实下载落盘和系统打印输出仍未执行，当前Browser仅公开visibility/viewport能力，没有已验证的外置下载目的地控制；不可让文件默认落本机或绕过会话抓取。优先继续业务单明细/轮次依赖恢复只读检查并收敛矩阵里的陈旧待办。语言命中问题已实页闭环，不重复列为未完成。目标active，整体未完成。

## 最新候选检查点：09-05 14:09（优先于下方旧检查点）

- 上轮与本轮均 PROGRESS：删除 preview 加 15 秒、execute 加 30 秒等待期限，覆盖响应头及 JSON 正文；超时释放操作，提交超时显示结果未确认，保留原请求及只读查询/原请求重试。不自动重删，迟到结果不误报成功。新增 4 条 API 挂起测试和 2 条实际组件+真实适配器测试；先红后绿。
- 检查报告 PDF（不是业务单双联生成器）修正中文句号、问叹号、右引号/书名号在满行时单独掉到下一行。原文不截断，金额/数量不变。增加三语 90 行责任说明完整性及签名分页验证；长说明原来已能分页，本轮补证据，不虚称重做。
- PDF 技能：实际生成器三语样本已重渲染，formal-pdf-v3 的中文3页、英文3页、对照5页共11页全部目视检查，页脚/签名/行序无遮挡，标点跟前字换行。都是外置 QA 样本，不是客户交付文件。producer formal-inspection-a4-v3。
- 新测试 filter 联合类型未收窄导致第一次 build/tsc 失败，改 flatMap 明确筛选 TextItem；随后范围 eslint、tsc、生产 build、diff 全通过。27 文件265组件、21前端单元通过，类型修正后15条PDF单元重跑通过。未改后台。
- 11:39 已受控部署 3220 PID87721，候选 apps/web cwd 核实；14:08监听仍同PID。实页IR1中文版/英文版/对照版均从“生成PDF文件”得到对应 ZH/EN/BI 文件名、1页 canvas 预览。语言切换会撤下旧文件。未点击实际下载或打印，不能宣称这两项已端到端验收。
- 上午实测1600×900/768×1024/480×844页面无横溢；切尺寸后立即读到旧canvas是异步绘制，等待后手机canvas382/容器408正常。下午浏览器缩放发生变化，实际视口512与320宽，正文506/314、canvas409/217，无外部横溢，不把请求的viewport参数当实测尺寸。已reset视口。一次手机check中英对照未选中；重新读DOM仍英文，后续新实页正常选中并生成BI，原因未证实，不能归因或称已修复。
- 数据核对：11:39 24表hash不变；14:09相对上午基线仅 business_order_document_revisions 41→42、business_order_document_snapshots 6→7 变化，其余22表相同。本任务此次只生成检查PDF浏览器blob，未调用业务单生成/保存或财务绩效/删除/客户通知/付费AI；不归因新增单据是谁操作，不回滚、不覆盖旧基线。后续先核对新增单据身份/时间再作新的只读验收基线。
- 下一步：真实单据下载/打印输出证据与三条路径整体矩阵；手机语言切换若再次复现，先截图与命中区域确认，再补回归。三条路径全量完成尚未获证明。用户明确续接后再次 get_goal 已确认 active；不另建目标/自动任务、不冒报完成。

## 最新候选检查点：09-05 10:52（优先于下方旧检查点）

- 上轮PROGRESS，本轮PROGRESS。删除提交前按已验证accountId写入sessionStorage恢复记录，保存原requestId/范围/原因/预览指纹，24小时有效；刷新详情恢复原请求而非重新生成或自动POST。暂存不是删除事实，金额/绩效数据未变。JSON字段顺序不影响同请求判定，真正修改范围/原因拒绝覆盖；格式/账号/时效/体积均检查，暂存失败不阻断已授权操作，明确本页保留并可重试本机暂存处理。
- AppShell新增按需出现的“删除进度”入口，原详情不存在时也能从其他正常页面打开，仅GET原回执、核对列表和人工移除本机提示。清理失败保留条目及重试。已验证成功的详情操作清理原账号恢复记录；不同账号看不到彼此条目。未引入全局自动删除或自动查询。
- 新useDeletionAccount沿用正式session读接口，15秒读取期限，监听既有wh:formal-session-changed。详情组件按账号/记录key隔离；红测发现仅mounted判断挡不住切账号与旧成功同tick到达造成旧导航，补generation同步失效检查。旧完成只清理原账号的原请求，不改变新账号UI。权限缺失/维修工不会显示删除入口。sessionStorage不是加密保险箱，也不保证跨设备/关闭标签后恢复。
- 26文件259组件、10前端删除单元、范围eslint、tsc、生产build、diff通过。本轮未改后端；上轮36后台删除测试通过。旧静态源码测试中的权限字面量已迁移到真实组件权限测试，保留其余既有静态检查。一次eslint effect同步setState及测试库无exact选项已修正再跑通过。
- 已部署3220 PID86306，候选apps/web cwd核实。真实IR1关联预览1600×900/768×1024高537，480×844高521，无横溢；关闭并reset视口。正常无暂存条目不占顶部空间。24表hash不变，无真实删除/创建/财务/绩效/通知/付费AI写入。恢复/故障/跨账号由隔离组件验证，未伪造实际浏览器存储；无新截图，不能说公共恢复弹窗完整目视实页验收已做。
- 下一步只处理真实剩余闭环：formal-record-deletions.ts的查询已有15秒期限，但preview/execute仍无期限，需复现头/正文挂起后的可恢复行为再修。随后回到PDF长责任与中文标点、外置下载及全三端矩阵，不停在删除单点。公共恢复仅24小时同标签；全路径完成未获证明，目标active。

## 最新候选检查点：09-05 10:42（优先于下方旧检查点）

- 本轮 PROGRESS：删除执行有同 requestId 幂等回执，但缺只读查询。新增 GET /api/formal/record-deletions/result：沿用现有删除权限，只读本次登录账号自己发起的回执；参数化主键查询、no-store，不调用执行/清理/审计写入，不新增索引或更改 schema。不存在回执只能判定未确认，不能证明没删除。
- 未确认/在途窗口增加“只查询删除结果”，保留原请求范围与原请求重试；查询匹配 requestId、根记录、全部删除范围及清理计数后才展示成功，查询自身不导航。15 秒期限覆盖响应头与 JSON 正文，迟到结果忽略，无自动 POST。
- 红测补出并修复：执行返回预览过期、重新检查后，旧在途查询错误会覆盖新确认说明；现在失效旧查询。覆盖关窗重开、重复查询、超时重读、未确认、账号隔离、无权限与错回执。25 文件247组件、8 文件36后端（PGlite内存）、10前端单元、范围eslint、tsc、build、diff通过。旧静态源码测试硬找“>删除<”与已有动态标签不符，改为核对三态标签，渲染行为已有组件断言。
- 已部署3220 PID85590，候选apps/web cwd确认；curl无会话读接口由中间件307转登录，不能误报为实会话查询成功。实页IR1只打开关联预览：1600×900/768×1024 高537，480×844高521，均无横溢，手机动作44px；仍提示已有客户沟通并给“查看相关记录”。关闭预览、reset视口。24表hash不变，无真实删除/绩效/财务/通知/付费AI写入；未确认故障路径仅隔离验证，无新截图，不把DOM尺寸当目视截图验收。
- 下一步：删除跨刷新恢复仍未实现。不要只把 sessionStorage 放进详情按钮，因为成功删除后原详情可能不存在，必须考虑列表/公共恢复入口和已验证账号隔离；利用本轮只读回执入口，不自动重发。PDF长责任/中文标点、外置下载与完整三端矩阵仍待做。目标active，上轮PROGRESS，本轮PROGRESS。

## 最新候选检查点：09-05 10:30（优先于下方旧检查点）

- 本轮 PROGRESS：四条组件红测复现列表与来源BO在currentAccountId变更后仍显示旧账号的在途检查文本、保持旧busy。创建窗口按账号key隔离；恢复hook提供本次挂载/账号周期scope检查，两处onCreated先验证scope。旧成功/失败只更新其账号的恢复存储，不关闭新草稿、不导航或额外刷新新账号。
- 另测账号1→2→1及卸载的旧scope始终无效。同账号读取失败曾因data=null让key变unknown清稿，红测后将最近验证的账号身份独立保存在列表响应状态，读失败不改身份。首次账号未返回不能开始无归属表单，显示确认状态；初读失败有重试，成功启用新建。列表返回缺失/非法accountId有明确重读错误，不把其当可创建身份。
- 24文件229组件、范围eslint（修正一次useMemo无效依赖警告后无警告）、tsc/build/diff通过。已部署3220 PID84984，cwd候选apps/web核实。实页捕获初始“正在确认当前账号”→可新建；1600×900/768×1024/480×844窗口无横溢，高868/977/844；关闭空窗口、视口reset、日志空。来源BO新建保留4321 AB · Nissan X-Trail，正常关闭。未真实切账号/创建/财务/绩效/删除/通知/付费AI写入，24表hash不变，无新截图。
- 上轮PROGRESS，本轮PROGRESS，目标active。下一步检查删除跨刷新恢复：入口apps/web/src/components/shared/record-delete-dialog.tsx，已有record-delete-recovery.test.tsx覆盖关窗重开和相同requestId重试，但目前父组件内存状态，需查明服务幂等契约及结果查询路线再持久化。不要执行真实删除。其后PDF长责任/标点/外置输出及全矩阵仍未完成。

## 最新候选检查点：09-05 10:21（优先于下方旧检查点）

- 本轮 PROGRESS：两个真实 API 适配器+组件红测复现创建请求响应头/JSON正文挂起无限等待。createFormalInspectionReport 全过程加入 30 秒期限，超时表达结果未知，释放操作、保留原稿、提供核对链接；AbortSignal 随 fetch，正文后检查 aborted，迟到合法响应不会导航/清稿/提示成功；无自动重发。
- 三条红测复现同车同班组但结论/维修工/备注不同的返回仍被当成功，现比对原提交；遵循服务端 NFKC+trim 规范，空备注/未指定维修工正常接受。已有错误ID/车/单/班组/断网/异常JSON回归保持。
- 恢复面板增加车辆身份、来源业务单新标签链接、按已填写车牌筛选的核对列表；红测验证不再只显示第几个尝试。没有改业务录入规则或创建服务幂等性。
- 24 文件 222 组件、33 检查API单元、范围 eslint、tsc、build、diff 通过。部署3220 PID84490，候选apps/web cwd核实。实页搜索4321AB命中原IR1；新建窗口三端1600×900/768×1024/480×844无横溢，内部可滚动，手机844px全屏；关闭空窗口、清搜索、reset视口，日志空，24表factsUnchanged=true。未真实创建/财务/绩效/删除/通知/付费AI写入，故障通过隔离组件验证，无新截图。
- 下一步优先验证旧账号在途检查创建响应是否会导航/提示到新账号（useInspectionCreationRecovery已分账号，但onCreated父回调仍需审查），再处理检查删除跨刷新结果恢复。PDF长责任/标点/外置输出及三条路径整体矩阵仍未完成，goal active。

## 最新候选检查点：09-05 10:15（优先于下方旧检查点）

- 本轮 PROGRESS：检查创建从仅父组件内存改为按已验证账号的 sessionStorage 暂存，24 小时有效。提交前同步记录 pending；刷新恢复为待核对，保留车辆、来源业务单、班组、维修工、结论及备注；成功/未知响应更新同一尝试。列表 GET 增加服务端 currentAccountId，业务单使用已有账号元数据。无账号或暂存失败退回本页恢复并提示。
- 不自动重新提交：源码确认创建服务没有幂等保护。恢复后原来源业务单继续随表单提交；提示不会因打开草稿就消失。清理失败保留重试；迟到旧页面响应读取最新存储，避免覆盖较新尝试。另条红测发现恢复的待核对条目会被后续新提交倒退成“保存中”，已修复快照合并。
- 24 文件 214 组件测试、4 接口测试、范围 eslint、tsc、生产 build、diff 检查通过。组件隔离验证刷新/账号与来源过滤/晚回包/清理失败，不宣称真实提交故障 E2E 已验收。
- 已部署 3220 PID84025，cwd 候选 apps/web 核实。1600×900 / 768×1024 / 480×844 新建窗口无横溢；桌面 868px 高内部可滚动，手机 844px 全屏。手机取消→继续编辑保留仅 QA 文本，再明确放弃；日志空、视口复原、24 表 factsUnchanged=true，未创建真实报告或写财务/绩效/删除/通知/付费 AI。重启期间第一次导航遇拒绝连接，服务恢复后新标签正常；没有新截图证据。
- 下一步：检查创建请求等待上限与异常响应确认、恢复提示显示车辆/来源便于人工核对、旧账号在途创建的界面隔离；后续检查删除跨刷新恢复、PDF 长责任/标点及外置输出验收。目标保持 active，尚未完成三条核心路径。

## 最新候选检查点：09-05 10:04（优先于下方旧检查点）

- 本轮PROGRESS：2条组件+1存储红测复现保存成功/放弃后removeItem失败无提示，旧暂存可能刷新复活。改为明确调用清理，自动暂存effect只写有目标草稿；失败独立页面提示和44px“重试清理本机草稿”，成功业务保存仍报成功，重试不POST。清理删除失败但可覆盖时写无内容null标记，防旧输入恢复；两者都失败则保留警告。新草稿开始覆盖原上下文，账号变化清旧清理提示。
- 23文件208组件、7数据刷新单元、eslint/tsc/build/diff通过，清理再次失败也保持重试；正常保存/取消/跨账户恢复回归未破坏。异常是隔离组件模拟，没有篡改真实浏览器storageAPI。
- 部署3220 PID83414，cwd候选apps/web核实。三端绩效窗口445px、原-500/空原因，无横溢；放弃刷新后无恢复入口/清理警告，日志空，viewport reset。24表hash不变，未真实绩效/财务/删除/通知/付费AI写入，无新截图。
- 实页已转到/orders/inspections，1条IR1。新建窗口填写仅QA文本后关闭不是直接丢失：会出现“放弃更改？”确认；继续编辑确实保留该文本，最后明确放弃验收文字退出，未创建记录。第一次误以为关闭已完成去点列表新建，超时后立即读snapshot发现确认界面并按真实按钮继续；不能将此断言成关闭丢稿bug。
- 下一轮从检查创建/待确认提交恢复入手，先读源码和故障测试，不重复修已有未保存确认。inspection-creation-recovery当前父组件内存数组，跨刷新待确认记录仍待处理；还需结合创建API是否幂等，禁止自动重发。检查删除持久恢复、PDF长责任/标点/安全外置下载与完整验收矩阵继续。目标active。

## 最新候选检查点：09-05 10:00（优先于下方旧检查点）

- 本轮PROGRESS：两条组件红测模拟同一BO组件在账户数据刷新后从账号1切到2；旧绩效请求在途会使新账号入口持续禁用。新增performanceScopeGeneration，账号变化/卸载递增；旧保存成功、失败、finally均检查所属scope，不能清新草稿、发布旧成功或解锁新在途请求。账号变化清绩效等待/核对/旧历史窗口与旧notice，仅旧绩效在途时释放其globalbusy。不修改服务端账户授权或真实session。
- 第三条回归确认旧账号迟到绩效历史不能打开到新账号页面、清掉新输入。两个账号各自sessionStorage草稿保留；账号1未确认状态不会被旧回包误清。205组件/23文件、7数据刷新单元、eslint、tsc、build、diff通过。
- 部署3220 PID83068，cwd候选apps/web核实。24表factsUnchanged=true；真实三端1600×900/768×1024/480×844绩效窗口445px，原-500/空原因，无横溢、日志空，放弃空稿退出并reset。真实账号未切换、未POST绩效/财务/删除/通知/付费AI，账号场景是隔离组件故障验证；无新截图。
- 下一项：sessionStorage.removeItem失败时当前放弃/保存成功会清UI但旧暂存可能下次复活，需用红测补清理失败可见重试及成功事实与本机清理分离，不用模糊“保存失败”代替。之后回到检查创建/删除持久恢复、PDF长责任/标点/外置下载与整体矩阵。目标active。

## 最新候选检查点：09-05 09:56（优先于下方旧检查点）

- 本轮PROGRESS。隔离PGlite红测证明setPerformanceDraft会忽略错误轮次ID，在版本相同的当前轮写入1250；路由红测证明ID未传递。修复required expectedRepairRoundId：前端发送原草稿roundId，API严格接受正安全整数number并转交，服务在lockCurrentRound的FOR UPDATE锁内匹配ID后才校验版本/状态与更新。原ID不符拒绝并提示保留草稿核对；旧客户端缺ID返回400明确刷新，不补猜当前ID。
- 不改绩效算法、收费、交单更正关联或数据库schema；更新所有测试服务调用必填ID。7条路由缺失/null/0/负数/小数/字符串/布尔ID测试确保不调用写服务；PGlite验证拒绝后当前轮全部字段及审计与之前一致。现有零值/负值/旧版本、交单更正、绩效汇总回归通过。
- 4后台文件73测试（含内存PGlite）、23文件202组件、7数据刷新单元、范围eslint、tsc、生产build与diff全部通过。前端既有E2E文件更新payload预期ID，未运行其浏览器套件，不宣称E2E通过。一次机械patch因截断行缺前缀未匹配，未写入；修正匹配完整行后应用。
- 部署3220 PID82715，cwd候选apps/web核实；只读响应校验三轮有效；24表factsUnchanged=true，historicalPendingCount0。真实绩效表单三尺寸1600×900/768×1024/480×844均445px高、无页横溢，原-500/空原因，放弃空草稿退出，日志空视口reset。未真实绩效/财务/删除/通知/付费AI写入；无新截图证据。服务端并发身份保障来自锁内代码+隔离拒绝测试，未跑真实多会话并发压测。
- 下一步：绩效账号在同一组件刷新变化时迟到写回隔离，以及sessionStorage异常清理恢复；之后回到检查创建/删除持久恢复、PDF长责任/标点/实页外置下载。目标active，不能因这条保护通过宣称三大路径完成。

## 最新候选检查点：09-05 09:51（优先于下方旧检查点）

- 上轮PROGRESS，本轮PROGRESS。源码确认业务单key已隔离不同BO的React状态，不把旧猜测当新bug；两条红测准确复现刷新/重挂载丢草稿与待核对状态，新增按currentAccountId+businessOrderId划分的sessionStorage恢复。仅同一标签页、最多24小时，zod结构/归属/大小/时效校验；暂存失败明确仅本页可用。无客户/财务明细缓存、无后台写表。
- 金额/原因/原轮次ID/版本/交单ID均保留；提交前同步暂存unconfirmed，刷新后展示先核对，不自动POST。卸载后迟到成功/失败不更新页面、不抹去待核对证据；放弃与验证成功清草稿。前端账号隔离不等于浏览器存储加密/共享电脑安全保证，未宣称跨设备或关闭标签后恢复。
- 新测试发现恢复旧轮次时新轮次版本可能恰好相同，会向新轮次提交旧金额；先红测再加前端原轮次ID/版本及正式交单有效性核对，变化时保留草稿给核对路线。服务端仍只校验当前轮次版本，下一轮最高优先级补expectedRepairRoundId与事务锁内校验，防读写间并发切换；不是全链路已完成。
- 5新增组件回归（恢复/未确认账号隔离/旧BO迟到/存储失败/同版本不同轮次）+6存储测试，23文件202组件通过；eslint、tsc、生产build、diff通过。部署3220 PID82210，cwd候选apps/web确认。24表factsUnchanged=true、historicalPendingCount0。
- 实页先填-500.25和QA未提交原因，真实导航刷新后恢复，三尺寸1600×900/768×1024/480×844对话框445px高，底部动作44px，无横溢；放弃后再刷新、打开显示原值-500/原因空，再放弃，日志空，viewport reset。未POST真实绩效/财务/删除/通知/付费AI；本轮无新截图，不把DOM尺寸验证当目视截图。
- 后续：服务端性能草稿轮次ID并发保护，账号在同组件刷新改变时旧在途回包隔离、sessionStorage异常清除边界；检查创建/删除持久恢复、PDF长责任/标点及外置下载继续。目标active。

## 最新候选检查点：09-05 09:44（优先于下方旧检查点）

- 本轮PROGRESS：真实组件调用实际API并让fetch不返回，两条红测复现保存/核对永久等待。新增withRequestDeadline：15秒维修轮次读取，30秒绩效草稿保存与已交单绩效更正；覆盖headers已到但JSON挂起。超时先报告结果未知再abort；不重试POST，不宣称回滚，不改变其他维修动作的写入等待规则。
- formalJson在解析后检查signal，防止迟到响应发布成功刷新。组件测试验证关闭窗口后超时仍显示、输入保留、核对按钮恢复、迟到成功不清稿不报成功、仅一次网络调用；四条辅助测试覆盖计时器清理、同步异常、abort错误优先级、正式更正版本/原因/requestId保留。
- 22文件191组件、7数据刷新单元、eslint、tsc、生产build、diff通过。部署PID81609，cwd候选apps/web确认。重启数据库尚未监听时只读脚本一次ECONNREFUSED，待服务就绪后重跑成功：readonly=true、三轮响应有效。24表hash不变。
- 实页1600×900、768×1024、480×844打开绩效窗，425px高、原值-500/空原因，44px底部按钮，无页面横向溢出/错误日志；放弃未修改草稿、视口reset，未提交真实POST。本轮未重试截图，无新目视截图证据，不把DOM验收当截图验收。
- 下一步：绩效跨BO/卸载迟到保存、跨刷新恢复仍未实现；当前明确仅本页保留，不能宣称持久恢复。只读轮次统一限时已覆盖删除预览复核，但其他写动作超时不在本轮范围。创建/删除持久恢复、PDF长责任/标点与实际外置下载继续排队，目标active。

## 最新候选检查点：09-05 09:36（优先于下方旧检查点）

- 本轮PROGRESS：四条真实BO组件红测证明绩效POST 200但错误业务单/金额/版本未推进/缺result仍被当成功清稿。新增formal-performance-response.ts在setRounds与成功提示之前校验，不改后台算法或POST调用；不合格响应保留草稿并进入已有核对路线。
- 校验当前轮次、历史、回单、审计、删除预览的渲染必需结构及业务单归属；历史schema按真实后端listRepairRounds不要求其不存在的intake字段，避免信任前端历史extendsCurrent的不准确类型。草稿结果匹配轮次/版本推进/金额；交单更正核对新result归属、correctsFormalHandoffId、旧交单已取消及有效替代金额，允许更正历史轮次而非强制当前轮次。只读绩效历史核对也在写入state前验响应。
- 新只读QA脚本scripts/qa/check-performance-response.ts使用default_transaction_read_only=on并核查开关，调用实际四个读取服务后按API JSON序列化；BO1三轮历史通过，无导出客户数据/调用写服务。不能把此读取契约验证当实际绩效POST成功验收。
- 新增4组件异常200回归+14纯响应契约测试，共21文件185测试通过；范围eslint、tsc、生产build、diff通过。测试any曾eslint失败，改unknown后重跑eslint/tsc及14项契约通过；部署无此lint错误版本源码变化，仅测试类型修正。
- 3220新PID81193，cwd候选apps/web确认；1600×900、768×1024、480×844真实绩效窗口425px高，原值-500/空原因，手机动作44px，无页横溢、日志空；未输入/提交真实调整，放弃空草稿退出，视口reset。24表factsUnchanged=true。此次无新截图证据，截图问题仍未解决。
- 下一步：绩效请求超时、跨BO/卸载迟到保存与刷新恢复；应优先补只读超时/不确定写结果核对及浏览器隔离故障矩阵。新校验只用于绩效保存和绩效核对，不代表所有维修轮次API已有契约保护。全局formalJson仍会把非法JSON回退{}，其他操作尚待审计。删除/创建持久恢复、PDF长责任/禁则/外置下载仍在范围内。目标active。

## 最新候选检查点：09-05 09:29（优先于下方旧检查点）

- 上轮PROGRESS，本轮PROGRESS：真实BO1打开已交单绩效表单，源码与两条组件红测复现关闭丢输入、同一事件循环提交两次发两笔请求。保留绩效目标/金额/原因在父页面，窗口开关独立；在途ref去重，等待时输入只读但可关闭，页面有查看绩效调整入口，失败不会藏在关闭窗口里。
- 新增只读核对最新绩效记录入口，失败时提示先核对再决定是否重试；保留原轮次/版本/交单ID，不静默换目标或覆盖草稿。历史打开后可返回原稿；若原事实变化，提示放弃原稿从最新记录开始。关闭核对后忽略迟到响应。未增加后台自动重试、未修改绩效算法/服务端权限。
- 五条本轮新增回归：关闭保留、已交单原因/错误保留+历史读取、背景成功、背景失败去重、关闭忽略迟到历史。前两条初始红测准确复现；新增历史测试还抓到原当前交单按钮清错误，已移除。测试fetchRounds统一beforeEach重置，避免失败测试未消耗的一次性响应污染后测。最终20文件167组件、范围eslint、生产build/diff通过。
- 3220部署PID80662，cwd候选apps/web确认。480×844/768×1024/1600×900真实表单425px高、页面无横溢；金额-500.25及QA原因在关闭/重开、尺寸切换后保留。底部动作44px，手机关闭图标44px、桌面36px。最后放弃验收草稿，重开仍原值-500、原因空，再放弃；无真实POST绩效/财务/删除/通知/付费AI，24表hash不变。日志空，视口reset。截图仍失败，无新增87号落盘目视证据。
- 下一轮应补独立Vite浏览器故障入口或实际组件更强网络层契约：绩效结果异常200、返回错误轮次、卸载/换BO后的迟到保存和跨刷新恢复仍未覆盖；当前保存/只读复核没有超时。当前只保留本页并明确离开刷新丢失，不宣称持久闭环。轮次删除旧preview/超时、删除与创建持久恢复、PDF长责任/标点/外置下载继续目标范围。目标active。

## 最新候选检查点：09-05 09:22（优先于下方旧检查点）

- 本轮 PROGRESS：真实 BO1 手机发现删除轮次窗口只有阻断文本、无后续入口，并把 round3 已交单的 −JMD500 误标为尚未计入的绩效草稿。先两条组件红测，再加只读入口：交单/绩效历史、正式单据与更正版本、本单相关检查结果；正式单据阻断明确仅取消交单不能解除。未改后端留存/删除规则，不能声称删除全部闭环。
- 新增重新检查关联记录：读失败原地提示并重试，检查时禁止删除，关闭仍可用；关闭/重开忽略旧响应，检查发现当前轮次变化则不替换删除目标，要求刷新。刷新确认编号清空，需要重新确认。只有 eligible 才显示未交单绩效草稿清理提示。
- 20 文件162组件通过、范围 eslint、生产 build、diff 通过。tsc 首次因新增测试 getByRole 不支持 exact 失败，去掉后生产 build 包含类型检查通过。最新 PID80144，cwd核实候选 apps/web；24表 factsUnchanged=true、historicalPendingCount0。
- 实页 480×844/768×1024/1600×900 无横向溢出，窗口812/798/798px，新增按钮均44px；重新检查成功、历史打开/返回、单据链接到 MEC-20260905-0001 / R5 并显示历史收费版本提醒。日志空、视口已reset。一次截图返回图像但未正确转交目视/保存，随后保存截图均Unable to capture screenshot；无87号成功落盘证据，不宣称截图视觉验收。
- 下一步绩效调整窗口：当前submitPerformanceDraft仍全局busy，关闭时未保留输入/原因，需隔离红测验证关闭后迟到失败、重复提交及结果核对；不要在真实BO上提交。另有只读复核无超时、关闭重开用旧preview、刷新/卸载恢复等未完成边界。删除/创建跨刷新持久恢复、PDF超长责任备注/标点/实际外置下载落盘继续队列。目标active，无真实财务/绩效/删除/通知/付费AI写入。

## 最新候选检查点：09-05 09:10（优先于下方旧检查点）

- 上轮分类PROGRESS；本轮先恢复实页：browser.tabs.list显示旧42已不存在，按Browser规则从同一browser新建有效标签，未切换浏览器或绕过data:策略。PID78786实页IR1三尺寸验证实际PDF：1600×900画布1964×2778，768×1024英文1308×1849，480×844对照764×1080；文件名ZH/EN/BI正确，页宽等于视口，44px打印/放大按钮，放大135→175有效，切语言旧链接/canvas消失，旧HTML打印入口隐藏。日志空。截图仍Unable to capture screenshot，不能宣称实页视觉通过；视口已reset。
- 新修formal-inspection-pdf v2：超过6行的描述转为紧接收费行的全宽说明，续页重复带序号项目引用与续标识，原收费行JOB/数量/金额只显示一次。长项目名称原文仍完整在收费行，说明区只用最多两行带序号的紧凑引用。三语续页身份回归先红后绿，全文末尾/坐标/A4保留。
- PDF实际重渲发现长中文末页只有签字线，新增真实PDF红测后将能装入一页的正常总额/责任备注/签字块一起分页，避免该已复現样本的孤立签字页。超一整页的责任备注仍允许分段，不宣称所有超长备注边界已覆盖。
- 样本在外置QA/formal-pdf-v2：normal三语各1页；long zh3/en3/bi4（前版zh4/en6/bi10）。长三语所有页均目视，调整签字后final-zh-2/3、final-bi-4、final-en-3、final-normal-bi-1重渲检查；长中文最后为总额/责任/签字而非空白签字页。旧long-zh-3.png是改签字前QA图，最新看final-*。中文标点偶尔行首仍是待优化排版问题，不丢字。
- 最终158组件、14模型/PDF/打印测试、范围eslint、生产build、diff通过。构建曾因新增测试fixture缺必填nullable字段失败，补齐后重跑生产构建通过；未部署失败构建。部署PID79655，候选apps/web核实；部署后IR1重新加载、PDF生成和canvas回归，24表factsUnchanged=true，historicalPendingCount0。无真实财务/绩效/删除/通知/付费AI写入。
- 尚待：实际PDF预览的浏览器截图、隔离长页翻页和失败重试、安全外置下载落盘、原生打印输出。不要把Poppler样本当实页下载或打印验收。下一轮可优先完成长期欠缺的BO轮次无效记录处理/绩效调整移动端与IR删除恢复路径的隔离验收，回到三条核心路径；报告超长责任备注、标点禁则和超长名称续页引用继续列队。目标active，本轮不是全部完成。

## 最新候选检查点：09-05 09:02（优先于下方旧检查点）

- 本轮 PROGRESS：InspectionPdfDownload 生成后直接用 PdfCanvasPreview 显示下载 Blob 的同一份字节，支持适宽、缩放和分页；原 HTML 阅读区与原 HTML 打印入口隐藏，切换语言/来源清掉旧 PDF 后恢复。新增“打印此 PDF”，调用同字节打印服务。预览失败保留下载，提供重新加载预览，不重新生成文件。
- 打印服务新增可选 AbortSignal：准备完成后发现已取消则释放打印副本，不打开原生打印；切换语言/来源卸载时取消旧打印请求，防旧语言迟到打印。原不传 signal 调用兼容。取消不是终止 PDF.js 正在进行的 CPU 渲染，只保证准备后不再启动打印并清理。
- 先红后绿覆盖同字节预览/重载和准备阶段取消；补同字节打印、失败后下载/重试、切语言取消。最终20文件158组件测试、4打印单元测试、tsc、范围eslint、diff通过；本轮源代码生产 build 通过后部署，追加的两条测试也通过tsc。PID78786、cwd候选apps/web核实。
- 09:01只读24表审计 factsUnchanged=true，historicalPendingCount=0。无真实业务数据写入、付费AI、删除或客户通知。
- **本轮实页验收未完成**：重启后过早打开标签42产生 ERR_CONNECTION_REFUSED；服务恢复监听后，该错误页被浏览器工具以 data: URL policy 拒绝访问/导航。没有绕过策略，没有把代码测试当作实页通过。新截图、实际PDF canvas三尺寸、原HTML隐藏、原生打印启动仍待验证；旧08:54生成链接结果不代表本轮预览验收。最终下载外置落盘能力也待明确，不点击到本机Downloads。
- 下一轮首先恢复正常受支持的浏览器实页访问并验收此次集成；用隔离长报告验证翻页/缩放/资源失败重试。随后改善长描述续页项目身份和狭窄描述列的版面利用，继续BO签字、轮次/删除恢复矩阵。目标active，不能标为全部完成。

## 最新候选检查点：09-05 08:54（优先于下方旧检查点）

- 本轮PROGRESS：新增formal-inspection-document.ts输出模型、formal-inspection-pdf.ts独立PDF渲染、formal-inspection-pdf-download.ts资源/生成适配器、InspectionPdfDownload真实入口。复用当前报价金额完整性/单位/status函数到HTML，保持待补不是0，未提供英文标待翻译，不复用旧ir-pdf退款合同。PDF用现有WH真logo、公司identity，三语言、完整结论/建议/备注、工时配件其他分类和合计，JOB原数量，金额单行；长行按页分段、续页表头、页码，金额仅原行首段显示。
- 真实入口“生成PDF文件”→生成中/取消→“下载PDF”，失败可原地重试；静态资源15秒上限，动态加载PDF库；切换语言/来源卸载清理blob并中止旧结果，防重复。页内保存blob，未做跨刷新PDF缓存。模型/渲染/组件新测试先红（缺模块）后绿，另实渲染发现普通英文/对照版签字另起一页，调整汇总/间距和按真实签字最后基线预留，加入单页对照回归。
- 最终全组件20文件155项、模型/PDF6项、范围eslint、生产build、diff通过。真实部署PID78380、cwd候选apps/web核实。24表hash不变、historicalPendingCount0；不写业务数据，不调用付费AI、不通知客户。
- 外置formal-pdf-samples.ts生成6份只读内存样本在QA/formal-pdf：normal zh/en/bi均1页，long zh4/en6/bi10页。Noto字体和WH logo真实嵌入，PDF文字/坐标边界、末尾标记、A4尺寸测试；Poppler重渲普通三版及长报告，normal-bilingual-final.png为最新单页图。旧normal-bilingual-2.png、long-zh-5.png、long-en-7.png等是迭代遗留QA图片，不得误当最新PDF页。长对照各页和长中文/英文首尾目视无裁字重叠；超长描述占狭窄列导致较多页，仍有优化空间。
- 实页IR1在1600×900/768×1024/480×844分别生成ZH/EN/BI blob下载链接，名称IR-20260829-0001-*.pdf，44px，page width=viewport，无alert/log。换语言旧链接消失。**没有点击浏览器最终保存链接**：默认Downloads不是外置软链，当前工具无已知指定保存位置能力；不能宣称落盘已验。同renderer样本真实外置落盘与解析已过。浏览器截图两次Unable to capture screenshot，未获得87及之后新截图，不声称实页视觉通过；viewport已reset。旧groupLive标签39失效，用原browser新建pdfLive恢复，没换浏览器。
- 下一优先：验证最终下载保存到外置卷的安全路径、统一实际PDF预览与HTML屏幕版式（目前金额规则相同但布局不同），补本轮入口视觉截图与资源失败/取消的实页隔离测试。PDF超长描述续页应明确项目身份并改善窄列空白，后续再优化。其他BO签字预留、轮次/删除恢复继续。目标active，不能把新增下载入口当全部闭环。

## 最新候选检查点：09-05 08:41（优先于下方旧检查点）

- 本轮PROGRESS：正式检查报告旧报价表混排；现在固定按工时、配件/材料、其他费用分组，每组独立表头和折后分类合计。无项目的分类不出现；other待确认项目保留分类待确认。中文/English/对照版分类标签分别显示。复用现有报价数字汇总及完整性判断，不更改价格、折扣、分类事实或后台规则。
- 2条红测复现缺分类，另加空分类回归，覆盖逆序输入、多个工时、本项折扣、整单优惠只在总价扣一次、缺价待补、明确零元、JOB、语言切换。全部19文件152组件通过，范围eslint、生产build、diff通过。部署PID77328/candidate apps/web，实际IR1工时15000、配件800，分类出现且合计15800不变。
- 隔离quote-status样本加入明确免收的other，只读GET、其余405。84桌面1600×900、85平板768×1024英文适宽、86手机480×844对照100%已目视；手机纸面794px在408px区域内横滑，page474无页面横溢，点击可见分类金额后scrollLeft332，所有金额nowrap，日志空。24表hash仍未变、historicalPendingCount0。服务已停、视口reset，无业务写入。
- 下一步优先当前formal检查报告真正可下载PDF：HTML报告分组已完成，不再重复做；实际宿主系统打印无响应问题仅有恢复提示，最终PDF/长页分页未验收，仍未闭环。旧ir-pdf.ts合同不匹配，不能直接强转/把待补变0/带旧退款话术。继续沿用当前分类/金额完整性规则，先测试输出数据模型再接PDF渲染与下载失败恢复。其余BO签字区预留、轮次/删除恢复保持队列。目标active。

## 最新候选检查点：09-05 08:34（优先于下方旧检查点）

- 本轮PROGRESS：检查报告直接window.print无失败反馈，纸张处于多层overflow/固定高度容器，且组件长期注入全局print visibility规则。新增inspection-html-print.ts：复制当前语言的完整sheet到body直系独立打印副本，读取缩放1.5不影响副本zoom1；打印A4每页12mm边距/186mm内容区，打印态隐藏其余body子节点，普通页不受样式影响。beforeprint只表示进入打印；afterprint/异常/3秒无启动/卸载清副本样式并恢复标题。删除旧组件全局print规则，打印按钮44px、等待、防重复、失败可再试。
- 3条实际组件红测复现缺独立副本、抛错无恢复、静默无响应。最终全组件19文件149项、范围eslint、build/diff通过；首次eslint prefer-const timer已修后重跑全测试/build。部署PID76969、候选apps/web核实；24表hash未变，无业务写入。
- print-recovery.html/tsx外置隔离生产组件+quote-status只读内存API，用明确标识的模拟宿主验证失败/取消/重试。82桌面无响应、83手机异常截图目视；平板768×1024核对副本body直系、全文相同、zoom1，模拟取消后移除/原阅读1.5。三尺寸1600×900/768×1024/480×844，无横溢，日志空；服务已停，视口reset。
- **真实3220宿主结果**：点击系统打印后未收到beforeprint，3秒后显示“当前浏览器未启动系统打印…”；按钮恢复、副本已清理、标题复原、阅读1.5保留。没有发送实体打印任务，也没有获得最终PDF；不能声称纸张或长页分页已验收。Browser工具没有可用原生打印对话框/PDF导出控制，本轮未绕过它换浏览器。
- 下一优先：检查报告正式报价表仍混排、缺工时/配件分组和分类合计；以及真实可下载PDF输出路线，不能只靠建议换浏览器。旧ir-pdf.ts存在但强制仅配件可待报价、带退款等旧合同，不可直接套用到当前formal检查报告。其他PDF签字预留、轮次/删除/创建持久恢复继续。目标active。

## 最新候选检查点：09-05 08:28（优先于下方旧检查点）

- 本轮PROGRESS：recordReply无条件form.reset清掉等待时新输入、不清旧成功notice、无同步重复提交保护。3条实际组件红测复现后修：提交保存原始字符串快照，成功只清未改动表单，后改内容保留并提示尚未提交；开始清notice，同步ref防重，按钮显示正在记录且44px。
- 全量回归发现新的真实竞态：第一次成功引发的历史GET迟到，load.setError(null)抹掉第二次提交失败。新增可控迟到GET红测，再分离loadError与操作error；读取失败单独提供重新读取报告与历史入口，不影响操作失败或现有草稿。
- 最终全组件19文件146项、范围eslint、生产build/diff通过，部署PID76502、cwd候选apps/web已核实。24表hash不变，无真实回复/通知/财务/绩效写入。不是全部目标完成。
- reply-continuity三文件外置隔离入口引用真实组件/API适配器，仅内存通信写入，其余405。79平板失败原文、80手机等待新输入、81桌面迟到成功保留均目视，尺寸1600×900/768×1024/480×844，44px按钮、无页面横溢、日志空；等待时切标签后返回仍保留。3228已停、尺寸reset；真实IR1跟进入口只读回归。
- 已知边界：本页内存回复不跨刷新；通信接口未知提交结果仍需核对历史，没有新增幂等键。HTML检查报告打印输出/祖先裁切尚待验收，继续优先处理，随后PDF签字预留与轮次/删除/创建持久恢复。目标active。

## 最新候选检查点：09-05 08:21（优先于下方旧检查点）

- 本轮PROGRESS：检查正式报告旧max-w-full把A4压到416px，手机金额列49px且换行。新增CSS module独立滚动阅读区，固定210mm纸张，适合宽度/100%/150%三档，ResizeObserver随区域宽度调整适宽，金额nowrap，44px动作。只修改报告阅读展示；原报告内容、保存和打印动作不变，print样式显式还原zoom1和A4。
- 实际组件缺阅读区红测后修复，新增ResizeObserver缩放回归；全组件19文件142项、范围eslint、生产build、diff通过。部署3220 PID76017，cwd候选apps/web；24表hash未变，historicalPendingCount0，无真实写入或付费AI。
- 外置quote-status隔离实际组件只GET内存样本、其他API405。76手机150%横滑、77平板适宽双语、78桌面100%截图均已目视；实页480宽再次验证sheet793.6875/reader408/scroll794/page480，所有data-money nowrap，三档操作正常，日志空。隔离服务已停，尺寸reset。
- 未验收实体打印或检查报告最终PDF页数；检查报告仍直接HTML window.print，新增阅读区print规则不等于全部祖先裁切风险已排除。下一步优先检查报告打印输出及recordReply异步恢复，PDF签字预留、轮次/删除/创建持久恢复仍待办。目标active。

## 最新候选检查点：09-05 08:14（优先于下方旧检查点）

- 本轮PROGRESS：报价数字汇总把null折算0，编辑器/查看页/正式报告错误显示完整或免费报价。增加纯显示资格判断，不改共享计算、数据库或保存格式。pending、entered但缺项目金额/无项目均显示报价待补；not_quoted显示本次不报价；明确0元和仅提供明确行总价均保留。已知分类合计保留，含未知行的分类/总计待补，显示状态徽标同步；英文报告未知单价/小计显示Pending。
- 四条组件红测后修复，加明确行总价及混合缺价覆盖、三状态英文断言。全组件19文件140项通过，范围eslint、最终build/diff通过。中途测试使用Testing Library不支持的exact选项导致build失败，去掉后最终build通过。3220 PID75585，24表hash未变，真实IR1只读回归；未写真实财务/绩效/报价/通知记录。
- 外置quote-status三文件隔离API仅GET内存样本，其余405，引用真实生产组件。截图73桌面1600×900、74平板768×1024、75手机480×844已目视；最初74/75可视窗口截图空白，改fullPage重新捕获覆盖。零元输入使完整报价正确显示125，正式报告仍读已保存样本、总额待补。QA已停，视口reset。
- 下一优先：75手机完整报告揭示A4内容被压成窄表格，JMD与金额换行，需报告独立横向阅读/缩放且打印不变。不要将手机无页面横溢当作可读性通过。其余recordReply旧notice、PDF签字预留、轮次/删除/创建持久恢复继续。目标active。

## 09-05 08:04 检查点

- 本轮PROGRESS：三条实际组件红测复现AI迟到覆盖后续人工编辑、撤销整理退回数据库版本丢人工稿、无停止入口。现在request序号+AbortController保护；当前草稿变化则暂存建议，复用完整结论/报价核对快照，人工应用后才进入草稿；撤销回到应用前人工稿。等待提供动画/停止，停止或卸载后的迟到结果无效，旧请求不会结束新请求的等待状态；失败保留草稿与指令。未修改AI事实提取或价格推断规则。
- 全组件135项（含逆序重试及失败保留）、API/报价单元48项、范围eslint与最终生产build通过。最后补AI按钮在其他保存期间disabled，与已有busy拒绝逻辑一致，后全组件/build再过。24表hash未变，无真实AI/财务/绩效/删除/通知写入。
- ai-continuity外置入口引用生产详情/API，/api/ai/chat只本机内存模拟，不调用模型，业务写入405。截图70桌面等待、71平板对照、72手机对照均目视；1600×900/768×1024/480×844，手机44px按钮且无横溢。实际操作已验证应用→撤销恢复Human revision during AI，停止→迟到无效→重试失败保留稿。QA服务已停止，尺寸reset；真实IR1只读回归正常、日志空。部署最终PID见验收矩阵。
- 停止仅取消客户端等待/请求，不能据此宣称供应商计算或计费已取消。AI暂存/撤销仅本页内存，不跨刷新；无草稿改动时仍沿用原来自动暂存到主工作区（不保存）的交互。接续报价编辑器pending显示0不一致、recordReply旧notice、PDF签字预留、轮次/删除与创建持久恢复。目标active。

## 09-05 07:57 检查点

- 本轮PROGRESS：报告保存等待时可继续编辑，旧响应却无条件setDraft覆盖新输入；两条实际组件红测分别复现内容回退及第二次保存失败仍显示旧成功。现在事件时草稿ref保留后续编辑，只推进已确认记录版本；提示后续修改尚未保存，不自动再提交。无后续编辑时仍采用保存结果。保存开始清除旧notice，加入重复在途提交防护。
- 19文件131项组件、范围eslint、生产build/diff通过。红测首次命令误用了不存在的vitest.components配置，改用实际vitest.workspace.config.mts后得到2失败17通过，再修为全绿。3220已部署PID74358，cwd候选apps/web；真实IR1编辑/完成编辑正常、日志空；只读24表hash未变。
- 外置save-continuity入口实际生产详情/API，全部API仅内存模拟：第一次PATCH延迟成功，第二次409失败。三尺寸截图67–69已目视，1600×900保存后保留新稿，768×1024失败清旧提示，480×844核对显示已提交/未提交两种内容，核对动作44px，页面无横溢。样式hash从实页重新读取，QA3228已停止，视口reset。未真实保存、付费AI、财务/绩效/通知写入。
- 下一轮优先AI整理迟到响应覆盖人工编辑风险（runAi当前无revision/请求序号保护，尚未红测）；本轮另发现报价编辑器pending仍展示折后JMD0，查看页/比较页却正确待补，需修显示一致性。recordReply亦未清旧notice但不属于本次已修声明。PDF签字预留、轮次/删除恢复及创建持久化继续待办，目标active。

## 09-05 07:50 检查点

- 本轮 PROGRESS：关闭检查创建等待窗口后，迟到失败原先随表单卸载而丢失。现由检查列表/业务单父页面保留逐次提交快照，显示等待、需要核对或已创建；失败可恢复车辆、班组、维修工、结论与备注，成功可打开报告。保留等待时填写第二份检查的现有行为，逆序完成不串稿，不自动重发。
- 先以真实父组件+表单两条失败复现，新增恢复、后台成功不关闭新稿及两次逆序失败覆盖；全组件19文件129项、范围eslint、生产build、diff通过。生产构建在最后一条测试补充前通过，生产代码未再改变。部署3220 PID73784，cwd候选apps/web。真实列表/业务单新建入口只打开和取消，日志空；24表hash未变。
- 外置create-recovery隔离入口引用实际生产列表/表单/API，中间件独占全部API且不接库。截图64/65/66最终为桌面1600×900、平板768×1024、手机480×844，手机恢复与核对按钮44px、sw480；恢复原稿完整。最初尺寸控制落在另一个标签页，发现后重建隔离标签并重测覆盖截图；最终以DOM尺寸核实。截图画布倍像素及外围空白不作为页面横溢证据。三张已目视，QA3228服务已停止。
- 边界：恢复队列只保留当前页面，离开/刷新会消失，界面明确提醒；未实现持久草稿/创建幂等性。下一轮检查保存期间继续编辑竞态及旧成功提示，随后PDF签字预留、绩效/失效轮次路线和删除跨刷新。真实3220回归标签当前为手机布局，隔离测试标签已reset；如需桌面验收先核对视口。目标active。

## 09-05 07:39 检查点

- 本轮 PROGRESS：检查报告保存异常200响应原先回退空对象并先setDetail，实际组件崩溃于workspace.versionNo。15条API失败+实际组件崩溃先复现；新增客户端详情响应schema，保存与核对读取均验证完整结构/报告身份，保存还核对版本+1及来源。不确定保存保留草稿，提示核对最新版本；网络/500不自动重试，409明确错误保留。
- 全组件19文件126项、检查API单元33项、范围eslint/生产build/diff通过。新组件测试实际经过保存响应截断、核对读取空对象、重新读取及采用确认版本，只有一次PATCH。首次绿测试末尾V1文本匹配多个元素，改为具体版本标识后全通过。
- 部署3220 PID73351，cwd候选apps/web；真实IR1及核对最新版本正常，日志空，恢复原生尺寸。24表hash仍不变，未实际保存/创建/删除/财务绩效/通知写入。新schema允许空可选资料及历史小数QTY读取，不修改业务校验或历史事实。
- 隔离save-response入口引用生产详情组件/API适配器，以中间件内存模拟200截断及GET失败后恢复，所有API不接数据库；桌面1600×900/平板768×1024/手机480×844截图61–63已目视，手机核对按钮44px、sw480，采用版本成功。一次截图捕获失败后同页重试成功，未重启服务。Vite有TS fixture将来native loader兼容提醒，不是生产错误；QA3228已关闭。
- 下一轮优先检查创建关闭等待后失败的反馈/草稿恢复，以及保存期间继续编辑和旧成功提示残留；PDF签字区过度预留、绩效/失效轮次路线及删除跨刷新继续待办。目标active。

## 09-05 07:31 检查点

- 本轮 PROGRESS：检查创建 API 对异常 201 JSON 原先回退 `{}`，实际表单误报创建成功并出现 `/orders/inspections/undefined`。先以 API 九条失败及真实组件失败复现，再校验返回 ID、编号、车辆、来源业务单和班组；网络/服务器不确定响应提示先核对列表，保留当前表单，不自动重试。核对链接另开标签页；明确 400 错误仍保留原信息。
- 补业务单来源后台完成专测：创建等待时关闭表单，完成后刷新原业务单相关检查列表、不突然跳页；这里生产回调原已正确，本轮补实际组件覆盖。全组件18文件125项、API单元17项、范围eslint、生产build通过。首轮build测试fixture类型缺字段已补正；根目录lint忽略Web文件后已在apps/web重新执行通过。
- 已部署候选3220 PID72795，实页来源车辆4321 AB、四班组及空表单关闭正常，日志空，页面宽2560无横溢。异常201验收只在隔离3228生产组件+真实API适配器中模拟，所有API由隔离中间件截断，不连接业务库。1600×900、768×1024、480×844截图58–60b已目视；手机核对入口/底部按钮44px，Tab可滚动到操作区，页面sw480。隔离服务已关闭。
- 07:31只读24表hash未变，无真实创建、财务、绩效、删除、通知写入。恢复只保证当前未卸载表单；创建尚无幂等键，关闭后失败及刷新恢复仍待处理。下一轮优先检查报告保存API同类异常响应与后台失败恢复，随后签字区过度预留、绩效/失效轮次路线。目标active，不称全系统完成。

## 09-05 07:18 检查点

- 本轮PROGRESS：PDF infoGrid明确slice(0,2)丢弃第三行以后信息，六条中英/维修工联的6行和90行真实PDF测试先失败（missing identity line 2），再改信息行自适应高度、跨页分片并标记续、后续元信息顺延。原34px短行保留，渲染器v20-complete-identity，不改历史文件。
- 全PDF46项、全组件17文件123项、范围eslint/生产build/diff通过。部署3220 PID72185，cwd已核对候选apps/web。24表hash未变，无真实财务/绩效/删除/生成单据/通知写入。
- 隔离脚本新增--identity及--identity --long，英文自然长名称3行和长车型3行完整，普通双联zh/en各2页；90行中英文身份压力样本各6页，四个PDF共16页PNG均目视完整。测试同时覆盖维修工联、每行文字出现次数/页脚边界/后续行间隔/双联逐页一致。未将90行样本写入真实客户，未新开3228验收服务器。前端没有本轮改动，实页只做已有历史联读取回归。
- 次要待检查：压力样本最后签字独占一页，需复核signatureBlock预留高度是否过于保守；这不等于整体美观验收通过。下一轮优先BO来源检查创建后台完成专测及创建API响应校验，随后绩效/轮次失效路线及删除跨刷新恢复。目标active。

## 09-05 07:12 检查点

- 本轮PROGRESS：长双联预览明确传copyLabels，按实际页数显示办公室/客户联页段；6/10页续页有联别，支持一键跳联和选择末页。奇数页不猜联别，提示逐页核对并返回历史版本；旧pageLabels单页模式保留。
- 另修实际版本识别缺陷：v18/v19名称不含-paired，之前被当历史单联。customer_copy从v16开始的配对契约现按版本代数识别；v15仍单联。先红后绿，新增六页导航、奇数页告警、v18/v19配对和v15历史回归，全组件17文件123项通过；范围eslint、生产build、diff通过。部署PID71525，3220候选apps/web。
- 外置隔离Vite3228直接引用生产PdfCanvasPreview，真实PDF.js加载上轮英文6页/中文10页样本，1600×900、768×1024放大150%、480×844跳到第10页验收，截图55–57均目视；手机44px联别按钮，页面sw480。3220已有CUS V20双联仍2个canvas/两个联别标题，客户页可跳，原生宽2560无横溢且日志空。未真实生成新单据或发送打印。隔离入口热更新createRoot重复警告补dispose/unmount，不涉及生产逻辑；验收后关闭3228进程。
- 07:12只读24表哈希仍未变。外置long-reader验收入口CSS链接来自当时3220构建hash，下次使用需先核对hash，不能当正式产品页。
- 下一轮优先PDF infoGrid两行截断长客户/车辆信息，随后BO来源检查创建后台完成专测及创建API响应校验；绩效/轮次失效路线及删除跨刷新恢复仍待续，目标active。

## 09-05 07:02 检查点

- 本轮PROGRESS：实页双联跳页/缩放后，隔离长样本复现PDF溢出。table/note/problem/signature原ensure只换一次页，90行文本继续画到页脚外。4类失败回归先红，再改按可用高度分片、续页重画章节/表头；金额仅首片，不重复计费。渲染器v19-long-content-pagination。中文句号独占下一行另有真实PDF失败回归，修复禁行首闭标点。
- PDF40项全过（含8项中英文长文本及1项标点）；普通费用双联仍2页。隔离脚本render-pending-quote.ts --long生成英文6页/中文10页，两联各占一半；16页PNG均目视，长说明无截断、分类合计/大总额/末页签字区存在。文件在外置QA目录，不写业务记录、不重生成真实历史联。
- 全组件首次117/118：收费重试测试在请求刚开始即点击仍disabled的取消，独立重跑通过，确认是测试没有等待拒绝后UI恢复。补等待可点击断言，未改收费生产逻辑；随后全组件连续两次118/118。范围eslint、生产build、diff通过。托管部署PID70911，已核对候选apps/web和3220。
- 实页桌面1600×900、平板768×1024、手机480×844测试已有费用双联、中英切换、客户联跳页、150%/适合宽度。手机页面sw480、reader cw430/sw893仅预览内部横滚、工具top777/bottom826可达。截图53平板、54手机；日志空，24表哈希未变。新超长PDF仅隔离渲染验收，未发实体打印。
- 新发现待办：PdfCanvasPreview只有pageLabels.length===pageCount才显示联别，传入两条标签时6/10页长单退化成第N页，需下一轮补按联分组与续页导航回归。infoGrid使用slice(0,2)，超长客户/车辆名可能截断，尚未修。继续BO来源后台创建专测/API响应校验、绩效及轮次失效路线；整体目标active。

## 09-05 06:50 检查点

- 删除提交后可退出，重开保留同一次 requestId、范围、指纹及原因；等待中不重复提交，失败沿用原 payload 重试。关闭不取消服务器删除。已确认 STALE 才重新预检。
- 删除成功与跳转失败分开，完成后有返回列表链接；后台完成不自动跳走。API 校验请求身份、根记录、完整删除范围及清理数量，异常响应不当作成功。
- 全组件17文件118项、删除API单元6项、后端删除隔离14项、范围ESLint及生产构建通过。部署PID70240，候选apps/web，3220。三尺寸预检/退出回归无横向溢出，手机按钮44px，截图52目视通过；日志空，24表真实数据哈希不变。未真实删除，执行中异常由组件模拟及隔离库验证。
- 提交快照仅在组件存活期间保留，刷新/卸载持久恢复未做；沟通事实作废政策未变。当前IR1客户跟进、无弹窗、原生尺寸。
- 下一轮优先长篇双联PDF/三尺寸缩放及末页动作统一验收，再补BO来源后台创建专测和创建API响应校验。目标active，本轮PROGRESS。下方06:45所列删除执行问题已由本轮处理，不重复修。

**最后更新：** 2026-08-28（Jamaica）\
**正式仓库：** `/Volumes/公司文件/Whole Hearted Car Service 正式系统`

每次继续开发时，按以下顺序读取：

1. `docs/superpowers/specs/2026-08-27-3210-canonical-single-app-design.md`：当前 3210 单体应用设计。
2. `docs/superpowers/specs/2026-08-25-single-formal-repository-rescue-design.md`：正式数据、权限和业务来源顺序。
3. `docs/architecture/FORMAL_SYSTEM_BUSINESS_BASELINE.md`：候选业务基线（未批准），仅用于保留候选条款来源。
4. `docs/acceptance/FORMAL_SYSTEM_END_TO_END_ACCEPTANCE.md`：候选端到端验收清单（未批准），不构成最终验收判定依据。

候选业务和验收材料发生冲突时，以用户明确发言、已批准设计和已批准分模块规格为准。

## 当前目标

以当前 3210 PC 系统为唯一应用界面，把正式服务与 PostgreSQL 能力并入同一个 Next.js 16 进程，形成可以由用户实际操作和复算的单店业务闭环：

```text
客户与车辆
→ Business Order 与维修轮次
→ Inspection Report
→ 收费版本与备注版本
→ 正式交单与绩效事实
→ 逐笔收款与双语 Receipt
→ 逐笔退款与双语退款签收单
→ 取车、挂账与停车
→ 经营概览、日报、月报与人话审计
```

## 当前修复顺序

1. 经营概览、今日/本周/本月/本年统计与目标口径全部读取正式后端。
2. 客户和车辆档案使用正式数据、完整编辑入口、正确归属与自动在场状态。
3. Business Order、收费、备注、维修轮次、收退款和打印以用户明确发言、已批准设计和已批准分模块规格为准形成闭环；候选业务基线材料仅作为待确认的候选输入。
4. Inspection Report 保持车辆中心的独立记录，并可选关联来源 Business Order。
5. 工作台、系统设置、基础字典和历史审计清除剩余临时数据入口。
6. 完成构建、自动测试、浏览器逐项验收和重启持久化验证。

## 续接纪律

- 先确认外置卷已挂载并可写。
- 只在本文件标明的正式仓库继续工作。
- 每完成一项，必须同时更新实现、自动测试和人工验收证据。
- 页面显示、正式接口和数据库事实必须一致；浏览器本地存储只允许保存界面偏好与刷新通知，不保存正式业务记录。
- 每次停止前，将已完成项、正在处理项、验证命令和剩余阻塞更新到本文件。

## 当前状态

- 当前 3210 界面已确定为唯一应用界面；候选版本在隔离工作区和 3220 端口开发与验收。
- 现有 3210、3211 和当前数据库在候选通过前保持不变。
- 已记录候选业务基线与候选端到端验收清单；二者尚未获得业务批准。
- 正在把现有正式接口改为单进程调用，并恢复经营概览与绩效目标计算。
- 3220 候选已新增正式“绩效参数”设置入口：全厂默认提成比例与 CNY→JMD 汇率按月版本化；维修组可按月设置特殊提成比例，并以空比例版本恢复全厂默认。所有版本均为 PostgreSQL 追加事实并写审计。
- 经营概览缺少月度绩效参数时，各维修组卡片直达 `/settings?team=<id>#performance-parameters` 并自动选中对应维修组；绩效页也提供同一入口。
- 2026-08-27 验证：正式后端 96 文件 / 336 测试通过，Web 963 单元测试通过，根与 Web 类型检查通过，生产构建通过；3220 浏览器确认设置入口、四个维修组、空值校验、直达预选和控制台无错误。候选数据库没有写入猜测参数。

## 2026-08-27 客户驾驶证采集交付

- **隔离候选工作区：** `/Volumes/公司文件/Whole Hearted Car Service 单体候选/3210-single-runtime`
- **候选分支：** `codex/3210-single-runtime`
- **候选入口：** `http://127.0.0.1:3220`
- **候选 PostgreSQL：** `127.0.0.1:55433`，数据与上传文件均位于候选工作区 `.runtime/`。

已实现并接入正式客户主档：

- 个人客户与公司客户都可在新建流程中选择驾驶证图片、拍摄或上传、识别后人工核对，再随客户事务一次提交。
- 公司证件明确归属主要联系人；公司注册地址和联系人证件地址分别保存、分别展示。
- 手机号、TRN 和驾驶证均为可后补资料；缺失时显示 `待补`，不会伪造完成事实。
- 证件状态固定为 `待补`、`待核验`、`已核验`、`需重新核验`；客户详情支持补录、替换和只读历史。
- 替换证件只追加新版本并封存旧版本；原始图片通过鉴权接口读取，不向页面暴露存储键。
- 超级管理员和前台管理员可写；老板身份保持全局只读。识别服务只返回姓名、出生日期、性别、地址四个安全字段，原始识别响应不进入数据库或审计。

隔离候选验收事实：

- 个人客户 `CUST-202608-0003` 已通过浏览器完成上传、人工核对、建档、受保护原件读取、刷新持久化和证件替换；主档姓名与地址随当前证件同步，旧记录保留为已替换历史。
- 公司客户 `COMP-202608-0002` 与主要联系人 `CUST-202608-0004` 已验证归属关系；公司地址为 `1 Company Avenue, Kingston`，联系人证件地址为 `77 Personal Lane, Spanish Town`，两者未互相覆盖。
- 候选数据库共有 3 条驾驶证记录和 3 份驾驶证原件；3 份文件的实际字节数与数据库 SHA-256 全部匹配。替换审计 1 条、核验审计 3 条，审计中未发现 Base64、原始 OCR 或供应商响应。
- 桌面和 430px 移动视口均无横向溢出；验收截图位于 `apps/web/docs/screenshots/formal-customer-license-*-20260827.png`。
- 最终回归：后端 101 文件 / 361 测试通过，Web 978/978 通过，协作边界 11/11 通过，根与 Web 类型检查通过，生产构建通过；lint 为 0 错误、1 条既有测试警告。

安全回退点：`backups/candidate-runtime-pre-license-20260827T1845-JM.tar.gz`，SHA-256 为 `b058e3e138b278d053bc2d6257ebee5342ac268f7a3b771210e010d50ff8d98d`。该备份只包含隔离候选运行时，不属于 Git 交付。

## 2026-08-27 正式记录删除交付

- **隔离候选工作区：** `/Volumes/公司文件/Whole Hearted Car Service 单体候选/3210-single-runtime`
- **候选分支：** `codex/3210-single-runtime`
- **候选入口：** `http://127.0.0.1:3220`
- **完整规格与验收：** `docs/superpowers/specs/2026-08-27-record-deletion-design.md`、`docs/acceptance/record-deletion.md`

客户档案、车辆档案、Business Order 和检查单详情已接入统一“删除”操作。超级管理员与前台可执行；其他角色不可见且接口拒绝。删除前由正式后端重新计算关联图，主记录逐项确认，从属记录按计数展示；存在真实业务事实时整体阻断，不做部分删除。

数据库主记录和原有 append-only 从属记录均要求同一事务中的请求编号、精确表名和精确行键授权。浏览器重试沿用同一个请求号，服务端使用事务级 advisory lock 串行化同号并发请求。当前车辆与历史车主关系都参与删除图。成功与拒绝分别写 `record.deleted` 和 `record.deletion_rejected`；自由文本补充说明不进入不可变审计，也不保存手机号、TRN、车牌、VIN、地址或证件内容。

3220 真实验收已完成页面删除、刷新持久性、详情 404、两次同号并发只执行一次、普通 SQL 删除被拒绝。验收测试客户与活动临时会话均为 0。最终回归：正式后端 110 文件 / 398 测试、Web 995/995、协作边界 11/11、删除 E2E 7/7、根与 Web 类型检查及正式生产构建全部通过。

## 2026-08-28 GitHub 与数据灾备

- **私有远程仓库：** `https://github.com/vicechancellor9999/whole-hearted-car-service`
- **稳定代码分支：** `codex/3210-single-runtime`，稳定提交 `f44cd1e`
- **稳定标签：** `checkpoint-2026-08-27-record-deletion`
- **AI 在建备份分支：** `codex/ai-service-wip-20260827`，首个在建快照提交 `6d7a01c`
- **加密数据灾备：** GitHub Release `data-backup-20260828T000230_EST`
- **本地加密包：** `/Volumes/公司文件/Whole Hearted Car Service 远程灾备/20260828T000230_EST/whole-hearted-runtime-20260828T000230_EST.tar.gz.enc`
- **加密包 SHA-256：** `f638ec8e7cc9d70ea31c73965b6bdab3af154ff0e76000e936b19437d12eb75e`
- **恢复密钥：** macOS 钥匙串服务 `whole-hearted-data-backup`，账号 `vicechancellor9999`；密钥不得写入仓库、文档或聊天。

数据灾备包含隔离候选的 PostgreSQL 物理快照、上传附件、AI 服务运行设置、根环境文件和 Web 环境文件。备份前数据库已正常停机；加密包已解密到独立目录，并在 `55434` 端口独立启动。恢复库与原库均为 46 张公共表、283 行，逐表数量指纹一致；随后恢复库已关闭，`3220/login` 返回 HTTP 200。验证材料位于同一 Release 的 `manifest.json`、`restore-verification.json` 和 `restored-database-summary.json`。

恢复时先下载 Release 的全部附件并核对 `.sha256`，从钥匙串读取密钥，只恢复到新的明确目录和备用端口；验证通过前禁止覆盖当前 `.runtime/postgresql`。原候选运行方式保持：

```bash
cd '/Volumes/公司文件/Whole Hearted Car Service 单体候选/3210-single-runtime'
pnpm start:candidate
```

后续提交纪律：每个功能使用独立 `codex/` 分支；实现、自动测试和浏览器验收完成后再提交并推送。每天结束、开始高风险改动前以及每个可验收节点都要推送。`.env.local`、`.runtime/`、`backups/`、构建缓存、测试结果和真实密钥不得进入 Git；运行数据继续通过加密 Release 单独灾备。

## 2026-08-28 Business Order 详情工作区优化

- **实现提交：** `c3e1dc4 feat: refine business order workspaces`
- **候选入口：** `http://127.0.0.1:3220/orders/business/1`
- **批准规格：** `docs/superpowers/specs/2026-08-28-business-order-detail-refinement-design.md`

已完成：

- 收款和退款改为可关闭的居中浮窗；收费项目的“编辑/保存”固定在同一标题操作位置，并明确显示“新增工时”“新增配件”“新增其他费用”“新增备注”。
- 三联工作区增加页面内正式单据预览、浏览器系统打印、新窗口打开，以及客户签字、服务照片、财务凭证和其他资料的 Business Order 附件中心；附件支持选择、拖拽和粘贴。
- 历史标签页改为紧凑时间线；字段前后变化默认收起，可按条展开。
- 沟通标签页改为评论流；保留 @、作者编辑和已编辑标记，并支持多张照片、粘贴照片和纯照片评论。照片只保存一次，再关联到评论和业务附件。
- 新增正式 PostgreSQL 附件表、鉴权读取接口、25 MB 文件边界和审计；删除误建业务单时同步清理评论、提及、附件关系和孤儿物理文件任务。

运行与保护事实：

- 启动器已成功应用 `0032_business_order_attachments` 和 `0033_record_deletion_collaboration_files`；候选库当前有 34 条 Drizzle 迁移，附件表和评论表均存在。
- 迁移前备份：`backups/pre-business-order-refinement-20260828T030838_EST.tar.gz`，SHA-256 `098a07fa902511b47971866e31b0614d8eb604a89c7b95eecbc2a92ef19575a3`。
- 验证：正式后端 115 文件 / 411 测试通过；Web 1005/1005 通过；本轮重点前端 32/32、后端附件与删除 15/15；根与 Web 类型检查、修改文件 lint、正式生产构建全部通过。
- 3220 已用生产构建重启，`/login` 返回 HTTP 200；未登录访问业务单和附件接口按预期跳转登录。
- 应用内浏览器控制通道在本轮验收时返回空列表，因此尚未记录人工视觉点击结论。下一次浏览器通道恢复后，优先验收四个标签、收退款浮窗、iframe 系统打印、附件上传/读取和评论照片发布。

## 2026-08-28 全系统平衡双主题交付

- **实现分支：** `codex/ai-service-wip-20260827`
- **候选入口：** `http://127.0.0.1:3220`
- **批准规格：** `docs/superpowers/specs/2026-08-28-balanced-dual-theme-design.md`
- **实现计划与验收记录：** `docs/superpowers/plans/2026-08-28-balanced-dual-theme.md`

主题入口固定为三种模式：`跟随系统`、`柔和亮色`、`舒适暗色`。新用户默认跟随系统；用户的模式选择保存在 `wh_theme_mode`，当前解析结果保存在 `wh_theme`。旧版 `wh_theme` 与 `wh_theme_source` 会在首次加载时迁移，不需要手工清理浏览器设置。跟随系统模式会监听操作系统主题变化，并在页面首次绘制前设置根节点主题，避免先亮后暗的闪烁。

应用颜色由 `apps/web/src/app/theme-tokens.css` 的语义角色统一管理。页面画布、侧栏、主卡片、嵌套区域、边界、文字、强调色和状态色使用同一层级逻辑；共享导航、页头、弹窗、卡片、表单、删除确认和 Business Order 四个工作区均已接入。历史页面通过应用壳范围内的语义兼容映射继承同一调色逻辑。三联正式单据及打印页继续使用白纸黑字的独立文档色板，浏览器打印和存 PDF 不受应用主题影响。

本轮实现提交顺序：

1. `c6e6968 feat: define system theme modes`
2. `8a4ffa1 feat: add three-mode theme control`
3. `1d46487 feat: add balanced semantic theme tokens`
4. `79d76fa feat: migrate shared chrome to semantic theme`
5. `d0adb4e feat: theme business order workspace`
6. `398f96f feat: balance legacy product surfaces`
7. `3b60282 test: align business order theme expectations`

交付验证：Web 全量单元测试 `1011/1011` 通过；主题与打印 E2E `12/12` 通过；主题与 Business Order 联合 E2E `18/18` 通过；Web 类型检查和生产构建通过；修改范围 lint 为 0 错误。生产构建已在 3220 重启，Next.js 监听进程工作目录为候选仓库 `apps/web`，PostgreSQL 继续监听 55433 并使用候选 `.runtime/postgresql`，`/login` 返回 HTTP 200。

用户复核后确认首版暗色仍缺少可见层级。根因不是主题开关，而是两项调色实现错误：暗色画布与主卡片明度过近；遗留 `dark:bg-slate-*` 工具类在 CSS 级联中仍可能保留原始深蓝黑色，并且 `slate-900` 曾被错误映射为侧栏色。修正后的暗色层级为 `#2B3037 → #3A424C → #46515D → #556271`，侧栏独立使用 `#24292F`；遗留工具类只在应用壳内强制映射到对应语义层，正式打印件不受影响。新增回归测试直接读取实际计算色，阻止内容卡片再次沉入侧栏或原始 `slate` 黑色。修正后主题与打印 E2E `13/13`、主题与 Business Order 联合 E2E `19/19`、Web 类型检查、修改测试 lint 和生产构建通过；3220 已切换到修正后的生产构建。

## 2026-08-28 账号设置入口集中

桌面侧栏底部和手机抽屉底部的账号区域现为统一账号设置入口。点击后以页面级浮窗显示当前账号、界面语言和外观模式；正式登录环境同时提供退出登录，Mock 预览环境保留身份切换。语言继续支持中文与 English，外观继续支持跟随系统、亮色和深色，原页面页头与经营概览页头不再重复显示语言或主题按钮。

浮窗支持点击外部关闭、Esc 关闭并将焦点返回触发按钮，也支持折叠侧栏和手机抽屉。控件使用现有语义主题层级，在亮色和深色模式下共享同一交互结构，不引入页面专用硬编码配色。

交付验证：账号设置、语言、主题与打印联合 E2E `17/17` 通过；Web 全量单元测试 `1011/1011` 通过；Web 类型检查、修改范围 lint、`git diff --check` 和生产构建通过。生产构建已在 `3220` 重启，`/login` 返回 HTTP 200，未登录业务单访问按预期跳转登录，Next.js 工作目录为本候选仓库 `apps/web`。应用内浏览器控制通道仍未提供可连接实例，因此本轮没有记录人工视觉点击结论。

## 2026-08-28 Business Order 历史、班组排序与 A4 单据

- Business Order 历史时间线按事件事实显示中文动作摘要。附件上传会显示附件类别、格式和大小；收退款、派单、打印修订等事件保留对应业务编号、金额或版本信息。
- 基础字典中的维修班组按全局顺序展示。超级管理员可拖拽、使用键盘上下移动并一次保存；排序写入 PostgreSQL `repair_teams.sort_order`，所有读取班组的页面共享同一顺序。
- 三联生成保存真实 PDF 文件及不可变版本记录。页面默认直接显示成品 PDF；下载与系统打印读取同一版本的相同字节。打印工具栏只保留版本选择、系统打印和 PDF 下载。
- PDF 渲染器 `bo-a4-v6` 使用正式汽修单据结构：企业抬头、客户与车辆事实表、收费/施工表、结算与收付款、备注、客户确认及签字区。色彩固定为白纸、深灰细线与企业蓝，打印内容不受应用主题影响。
- 已用 Poppler 逐页检查现有客户联、办公室签字留底联和维修工联最新版：三份均为 1 页，页面尺寸均为 `595.28 × 841.89 pt` A4；表格、金额、备注和签字区均在打印边界内。
- 候选库已应用迁移 `0034_repair_team_sort_order`、`0035_business_order_document_revisions`。历史三联已补齐 `bo-a4-v6` 最新版 R6；原始快照和每个文件版本继续保留。
- 迁移前可恢复备份：`/Volumes/公司文件/Whole Hearted Car Service 单体候选/backups/20260828-070705-EST-before-document-revisions`。`SHA256SUMS` 已验证通过，包含正常停机后的 PostgreSQL 与上传文件归档。
- 自动验证：A4 内容、PDF 渲染、版本冲突、数据库追加约束共 10/10；历史、班组排序与打印界面重点测试 34/34；根与 Web 类型检查、生产构建和 `git diff --check` 通过。Lint 为 0 错误、1 条既有测试警告。
- PDF 文件已经渲染为 PNG 进行人工图像检查。应用内浏览器控制模块本轮不可加载，因此页面点击验收仍需在 `http://127.0.0.1:3220` 复核。

## 2026-08-28 A4 单据双语文件与预览缩放

- **批准规格：** `docs/superpowers/specs/2026-08-28-business-document-language-and-zoom-design.md`
- **实现计划：** `docs/superpowers/plans/2026-08-28-business-document-language-and-zoom.md`
- **数据保护备份：** `/Volumes/公司文件/Whole Hearted Car Service 单体候选/backups/20260828-0834-EST-before-english-document-files`；PostgreSQL 与上传文件归档的 `SHA256SUMS` 均已验证通过。

客户联和办公室签字留底联的每个新修订现在同时冻结中文、英文两份独立 PDF 文件及各自 SHA-256；维修工联继续只生成中文文件。英文文件使用独立英文模板，收费项目、说明、单位、备注、收付款方式和确认文字都读取同一业务快照中的英文业务字段；缺少必需英文业务字段时，生成事务会在写入前整体拒绝，不保存不完整修订。读取接口、页面预览、系统打印和下载都按所选语言读取同一份不可变文件，下载名明确包含 `ZH` 或 `EN`。

页面预览默认按可用宽度完整显示 A4，不再使用固定放大倍数；工具栏提供缩小、当前百分比、放大、适合宽度和 100%，手动范围为 50%–200%。PDF.js 仍按屏幕像素密度设置 Canvas 后备像素，缩放只改变显示尺寸，不以降低输出分辨率换取适配；超出预览区域时在单据预览内部滚动。

候选库已应用迁移 `0036_business_order_document_english_files`。现有三联已补齐 `bo-a4-v8` 最新 R8：客户联和办公室联均为中英双文件，维修工联为中文文件；五份实际 PDF 均为单页 `595.28 × 841.89 pt` A4。英文客户联和办公室联已渲染为 PNG 检查，未发现表格截断、内容重叠或越过打印边界；PDF.js 全文提取确认两份英文文件均无中文字符。

本轮重点自动验证：后端 PDF 与双文件持久化 8/8、Web 单据与打印 29/29；全量后端在受控 4 个 worker 下 116 文件 / 420 测试通过，Web 全量 1024/1024、协作边界 11/11 通过；根与 Web 类型检查、生产构建及 `git diff --check` 均通过。3220 已用最终生产构建重启，`/login` 返回 HTTP 200，未登录单据页按预期跳转登录。应用内浏览器控制通道仍返回空实例，因此页面按钮的人工点击验收留给用户在 3220 当前登录会话复核。

## 2026-08-28 本机入口与登录状态统一

本机运行只使用 `127.0.0.1` 作为登录 Cookie、语言和主题偏好的唯一来源。访问 `http://localhost:3220/*` 会在路由与鉴权之前以 307 跳转到 `http://127.0.0.1:3220/*`，路径和查询参数保持不变；未来 3210 正式入口使用同样规则。切换亮色、深色、中文或 English 不创建新登录域，也不要求再次登录。

验证结果：入口规则单元测试 1/1、账号设置语言与主题 E2E 3/3、Web 类型检查、生产构建和 `git diff --check` 通过。生产构建已在 3220 重启；实际 `localhost:3220/login` 浏览器标签刷新后变为 `127.0.0.1:3220/login`，随后进入 `/` 继续使用原有 127 登录状态。候选 PostgreSQL 数据和附件均未修改。

## 2026-08-28 维修接车、回单与前台审核闭环

- **批准规格：** `docs/superpowers/specs/2026-08-28-work-return-review-closure-design.md`
- **实施计划：** `docs/superpowers/plans/2026-08-28-work-return-review-closure.md`
- **迁移前冷备份：** `/Volumes/公司文件/Whole Hearted Car Service 单体候选/backups/2026-08-28-2139-before-work-return-review`；PostgreSQL 和上传文件归档的 `SHA256SUMS` 已验证通过。

维修工账号登录后直接进入 `/mechanic` 手机工作台，只能看到本班组已派单、维修中和待审核任务；任务内容包含车辆、施工项目和维修说明，不包含费用承担方和收费金额。维修工拍摄里程照片并填写接车里程后完成接车；保存中断时可从已保存步骤继续。电子回单按项目记录已完成或未完成，并可提交实际完成情况、异常说明和维修照片，随后进入前台审核。

前台工作台现有独立“维修回单审核”队列，直接进入相应 Business Order。审核弹窗展示实际维修工、逐项结果、异常和附件，并填写本轮绩效值；“审核通过并正式交单”在同一事务中完成审核和正式交单。退回原因会回到维修工任务页。纸质回单从维修中的 Business Order 通过“收到纸质回单”登记，要求上传照片或 PDF、选择实际维修工、核对完成项目并填写绩效值；“确认并正式交单”在同一事务中记录纸质回单、现场核对和正式交单。

候选库已应用 `0038_work_return_review_closure`，`repair_round_work_returns` 已有来源、异常和逐项结果字段，回单附件关联表已存在，Drizzle 迁移总数为 39。审核与交单合并后的核心服务及接口自动测试为 48/48，通过路径和失败回滚路径均已覆盖。其余构建与浏览器验收结果记录在本节后续验证说明中。

最终验证：根项目与 Web 类型检查、修改范围 ESLint、生产构建和 `git diff --check` 均通过；3220 已用最新生产构建重启。应用内浏览器确认 Business Order 1 已处于“本轮维修已经正式交单”，绩效值为 JMD 54,400.00，工作台待审核数量为 0，页面无相关控制台错误。当前没有待审核或维修中的正式记录，因此未创建测试业务单来重复打开审核或纸质回单浮窗；这两个提交路径由上述 48 个服务与接口测试覆盖。

## 2026-08-29 检查结果录入、班组归属与车辆实时搜索

- **批准规格：** `docs/superpowers/specs/2026-08-29-inspection-intake-team-vehicle-search-design.md`
- **实施计划：** `docs/superpowers/plans/2026-08-29-inspection-intake-team-vehicle-search.md`
- **迁移前冷备份：** `/Volumes/公司文件/Whole Hearted Car Service 单体候选/backups/2026-08-29-0301-before-inspection-intake`；PostgreSQL 与上传文件归档均已通过 `SHA256SUMS` 校验。

新建检查结果改为车牌实时搜索：输入车牌后只返回匹配的在用车辆，不再预先加载完整车辆目录；选择结果后继续录入。无匹配车辆时直接打开正式车辆新建浮窗，车牌自动带入；车辆保存后自动回到原检查表单并选中刚创建的车辆，已填写内容继续保留。

检查结果现在必须选择提交班组；实际维修工为可选项，且只能从所选班组的在职维修工中选择，切换班组会清除不再匹配的维修工。录入区只保留一份必填检查结果和一份可选特殊情况备注。列表、详情、读取权限和写入权限都使用班组归属，维修工只能读取本班组范围内的检查结果。

候选库已应用 `0039_inspection_team_intake`。迁移完成后 `inspection_team_id` 为必填，实际维修工和特殊情况备注为可选；当前正式库没有既有检查结果需要补班组，也没有因迁移产生的悬空记录。

浏览器验收已在正式 3220 候选完成：`4321AB` 实时显示 Nissan X-Trail；不存在的 `ZZ-9999` 显示新建车辆入口，正式车辆浮窗自动带入该车牌；选择 `4321AB` 与车间一组后，维修工选项只显示该班组成员。验收没有提交检查结果或创建车辆，因此正式数据未增加测试记录。

最终自动验证：根项目 118 文件 / 446 项、Web 1038/1038、重点车辆与检查录入 35 项、协作边界 11/11 全部通过；根与 Web 类型检查、生产构建及 `git diff --check` 均通过。

检查结果新建浮窗的“提交班组”和“实际维修工”已由下拉菜单改为可直接点选的方块选项。桌面双列、手机单列；选中项显示企业蓝描边与勾选标记。维修工选项在选定班组后出现，并保留“不指定维修工”；切换班组会自动清除不属于新班组的维修工。浏览器已验证车间二组可选张真真、切换到车间一组后张真真消失且恢复“不指定维修工”，手机宽度无横向溢出。验收未提交表单，正式数据未发生变化。

本次界面调整最终验证：新增选择器测试与原检查录入测试 4/4、Web 全量 1039/1039、Web 类型检查、修改范围 ESLint、生产构建及 `git diff --check` 全部通过；最新生产构建继续监听 `127.0.0.1:3220`。

## 2026-08-29 A4 PDF 网页预览清晰度

单据原始 PDF、下载文件和系统打印未变更；本次只修正网页 PDF.js Canvas 的栅格化密度。原预览直接使用浏览器报告的 `devicePixelRatio`，当前应用内浏览器仅为约 1.1，导致 A4 细字与细线在放大预览中发虚。现将 Canvas 后备像素密度限制为最低 2 倍、最高 3 倍，同时保持原 CSS 显示尺寸与缩放比例不变。

浏览器在 `127.0.0.1:3220/orders/business/1?tab=documents` 实测：主预览由约 `869 × 1229` 后备像素提升为 `1579 × 2234`，显示尺寸仍约为 `790 × 1117`；切换到 100% 后仍保持每个 CSS 像素 2 个后备像素，控制台无错误或警告。最终验证：清晰度与原打印消费测试 7/7、Web 全量 1040/1040、Web 类型检查、生产构建及 `git diff --check` 通过；3220 已重启到最新生产构建。

## 2026-09-02 macOS 常驻候选服务

- **LaunchAgent：** `com.whcarservice.candidate-runtime`
- **用户入口：** `http://127.0.0.1:3220`
- **候选 PostgreSQL：** `127.0.0.1:55433`
- **外置卷服务目录：** `/Volumes/公司文件/Whole Hearted Car Service 单体候选/service`
- **本机登记文件：** `/Users/lijianfu/Library/LaunchAgents/com.whcarservice.candidate-runtime.plist`

候选运行时现由登录用户域的 LaunchAgent 常驻管理。启动器等待 `/Volumes/公司文件`、候选生产构建、环境文件和 Node 运行时全部可读写后，再从候选目录启动原有统一运行入口；异常退出由 launchd 自动重启，文件系统挂载事件也会触发重新启动。启动脚本、配置主源、测试和运行日志都位于外置卷。由于当前 macOS 的 `launchctl` 拒绝从外置卷文件或指向外置卷的软链接加载 plist，本机保留一份与外置卷配置主源逐字节一致的小型 plist；这是用户在 2026-09-02 明确批准的唯一例外。

验证事实：服务配置与挂载等待自动测试通过；LaunchAgent 状态为 `running`；3220 的 `next-server` 工作目录为本候选仓库 `apps/web`；55433 的 PostgreSQL 数据目录为本候选仓库 `.runtime/postgresql`。强制终止已核实身份的 3220 Web 子进程后，launchd 在约 4 秒内以新 Web 与新 PostgreSQL 进程恢复，`/login` 返回 HTTP 200。卸载并重新加载 LaunchAgent、且不执行手工 kickstart 的登录加载模拟也在约 4 秒内自动恢复。应用内浏览器复核 URL 为 `http://127.0.0.1:3220/login`、标题为 `Whole Hearted Management System`，登录页完整，中英文切换正常，控制台无错误或警告。本次未修改业务数据。

维护命令：用 `launchctl print gui/$(id -u)/com.whcarservice.candidate-runtime` 查看状态；用 `launchctl kickstart -k gui/$(id -u)/com.whcarservice.candidate-runtime` 做受控重启。日志位于 `/Volumes/公司文件/Whole Hearted Car Service 单体候选/service/logs/candidate-runtime.log`。LaunchAgent 已运行时不要再手工执行 `pnpm start:candidate`，以免同一端口出现重复启动竞争。

## 2026-09-02 Business Order 紧凑分类列表

Business Order 列表现在只在原业务信息旁补充四项运营事实：业务分类、维修内容概述、当前总价和当前班组。分类固定为“保养、维修、检查、返修”，前三类可同时存在；“返修”不需要人工勾选，从第 2 维修轮次起按轮次事实自动显示。列表顶部提供“全部、保养、维修、检查、返修”紧凑筛选按钮，筛选条件写入 URL，可刷新和翻页保留。

新建 Business Order 时，现有自然描述先交给系统 AI 返回封闭的多分类结果；AI 不可用或响应无效时，后端使用同一组保守关键词规则兜底，不影响建单。只允许三种基础分类进入数据库，未知分类不会保存。维修内容概述优先读取当前收费版本的项目名称，没有收费项目时依次使用当前轮次、Business Order 当前版和创建原文；价格读取当前收费版本总额，班组读取当前维修轮次。

候选库已应用迁移 `0049_business_order_categories` 和 `0050_conservative_business_order_categories`，迁移总数为 51。第二个迁移收紧了历史回填：否定描述和无关英文子串保持“待分类”，不把不确定内容写成业务事实。现有 `KGN-WH-2026082500001` 根据原收费项目补齐“保养、维修、检查”，其第 3 轮同时自动显示“返修”；原收费、维修轮次、客户、车辆和交单事实均未改写。实际页面显示 JMD 59,200.00、车间一组和完整维修概述，返修筛选已点击验证。

验证结果：后端全量 123 文件 / 482 项通过；新增分类、接口和紧凑列表聚焦测试 21/21 通过；根与 Web 类型检查、生产构建通过。Web 全量单元测试 1093/1094 通过，唯一失败仍是既有的跨月日期敏感停车测试 `pickup cohort remains frozen across later restore and unsubmit lifecycle changes`，与本次列表及分类改动无关。3220 已由 `com.whcarservice.candidate-runtime` 重启并继续由 launchd 管理；浏览器确认分类标签、内容概述、价格、班组和返修筛选均读取正式 PostgreSQL 数据。

## 2026-09-02 Business Order 详情紧凑工作区

Business Order 详情页头已经压缩为紧凑的编号与车辆信息行，维修进度仍完整保留。工作区标签现在紧接页头显示，首标签由“收费 · 收款 · 维修班组”改为“业务单明细”。问题描述已并入该标签的左侧明细区并位于收费项目之前：没有内容时显示“问题描述”标题和“修改”入口；已有确认内容时直接显示内容，并继续保留修改及版本历史能力。收费、收款和维修班组的业务逻辑及正式数据均未改动。

验证结果：紧凑布局与问题描述聚焦单元测试 8/8、空问题描述及仅有本轮问题的浏览器 E2E 2/2、Web 类型检查、修改范围 ESLint、生产构建和 `git diff --check` 均通过。3220 已由原 `com.whcarservice.candidate-runtime` LaunchAgent 受控重启；3220 监听进程确认为其候选运行时派生的 Next.js 进程。应用内浏览器确认 URL 与标题正确、标签顺序正确、空问题描述显示“问题描述”和“修改”，修改浮窗可正常打开和取消，且控制台没有错误或警告；验收未保存任何业务内容。

## 2026-09-02 收费项目编辑按钮误保存修复

“编辑收费项目”和“保存收费项目”原先复用同一个 DOM 按钮，并在点击过程中把 `type="button"` 切换为 `type="submit"`。当收费项目非空时，React 完成状态更新后浏览器继续执行同一次点击的默认提交动作，导致一次“编辑”点击直接保存一个未变更的新版本。现已把编辑和保存拆为带独立 key 的两个按钮节点，并加入一次性显式提交授权：点击编辑只装载草稿并显示编辑表单，在输入框按回车也不会保存，只有明确点击“保存收费项目”才调用原生表单校验并提交；中文输入法的回车确认不被拦截。

正式库只读核对确认，Business Order 1 在 2026-09-02 18:26:39–18:26:55 因该问题产生 V13–V20 共 8 个重复收费版本。逐项比对版本折扣、金额、收费项目和备注后，V13–V20 与 V12 完全一致，实际价格和业务内容没有改变；这些既有审计版本未删除或改写。

验证结果：浏览器 E2E 先稳定复现“点击编辑”、普通输入框回车以及收费行删空后“修改原因”回车的误提交路径，修复后确认三者均为 0 次写入、输入法组合回车不受阻止，明确点击保存恰好产生 1 次写入；相关界面单元测试 24/24、Web 类型检查、修改范围 ESLint、生产构建及 `git diff --check` 均通过。正式页面验收只执行编辑、回车和取消，不执行保存；收费版本在验收前后保持 V20。

收费项目编辑行的桌面布局同时修正：单位、数量、含税单价和本项折扣现在与项目名称及描述的中文首行顶部对齐；中英文双层输入结构以及右侧翻译、删除按钮位置不变。浏览器布局测试确认四个字段标题与项目名称标题的纵坐标一致。

## 2026-09-02 三联预览工作区高度对齐

三联文件列表和 PDF 预览不再使用 `720px` 的固定高度上限；桌面端会随 Business Order 工作区剩余高度伸展到与左侧导航栏相同的底线，长列表和 PDF 继续只在各自区域内部滚动。生成、下载、打印和不可变文件版本逻辑均未改动。

浏览器回归测试在 `1625 × 1000` 视口先复现内部区域距离工作区底部 363.5px，修正后确认内部区域填满扣除 16px 卡片内边距后的全部可用高度。

最终验证：该布局回归测试在 `1625 × 1000` 与 `1625 × 1200` 两种窗口高度均通过；相关 Business Order 与打印单元测试 36/36、Web 类型检查和生产构建通过。3220 已由原 `com.whcarservice.candidate-runtime` LaunchAgent 受控重启，应用内浏览器实测三联区与卡片可用底线误差为 1px（边框），PDF 预览已随工作区延伸到底部且控制台无错误或警告。验收前后 Business Order 1 的收费版本保持 V20，未生成单据或修改业务数据。

## 2026-09-02 业务附件紧凑卡片

业务附件列表由大幅 4:3 图片卡改为固定 96px 高的横向卡片：桌面卡片宽 280px，左侧保留 112px 宽的可辨认缩略图，右侧文字区约 166px，只显示附件类别、截断后的文件名或说明、上传人和时间；完整名称通过悬停标题保留。窄屏卡片自动占满可用宽度，点击后仍打开原始附件。

上传区新增“选择附件”和“调用摄像头”两个明确入口。“调用摄像头”通过浏览器标准 `MediaDevices.getUserMedia()` 请求高分辨率后置摄像头画面，可预览、关闭取景或“拍照并上传”；高拍仪只要在 macOS 中作为标准摄像头设备出现即可复用该入口。拍下的 JPEG 继续使用当前附件类别、可选说明以及原有 multipart 附件上传接口，不新增数据库字段或独立存储链路。

布局回归测试先复现正式卡片高 356px、其中图片高 240px，随后复现横向卡片仍宽 435px、文字区过宽；修正后实测卡片 `280 × 96px`、缩略图 `112 × 94px`、文字区 `166px`。上传、附件读取、权限及业务数据结构未改动。

最终验证：紧凑卡片与摄像头打开/关闭 E2E 2/2、附件及共用摄像头相关单元测试 15/15、Web 类型检查和生产构建通过。3220 已由 `com.whcarservice.candidate-runtime` 受控重启；应用内浏览器在 `1625 × 1514` 实测按钮与两张紧凑卡片均正常，附件类别切换正常且控制台无错误或警告。Business Order 1 仍有 2 份附件，收费版本仍为 V20，本轮未上传、删除或修改业务数据。

## 2026-09-02 检查报告报价折扣结构

检查报告报价已与 Business Order 收费项目采用同一组财务字段：项目名称、描述、单位、数量、含税单价、本项折扣和小计。工时、配件和其他费用分别汇总；总计区同时显示原报价、本项折扣合计、整单优惠和折后报价。报价编辑区可分别填写每行本项折扣与整单优惠，行小计按“含税单价 × 数量 − 本项折扣”自动计算，最终报价再减整单优惠；折扣不能超过对应项目或整单的小计。正式 A4 检查报告同步显示相同的价格与折扣结构。

两个折扣字段保存在既有检查报告工作区版本的 JSONB 报价快照中，继续使用只追加版本，不需要数据库迁移。旧版本缺少字段时读取为 0，不回写、不改写历史。AI 只能保留原文明确给出的折扣；原文没有折扣时必须返回 0，不能自行创造优惠。

验证结果：检查报价计算与界面单元测试 25/25、检查报告工作区服务集成测试 13/13、折扣展示/编辑/打印 E2E 2/2 通过；根与 Web 类型检查、修改范围 ESLint、生产构建和 `git diff --check` 通过。后端全量 482/483 通过，唯一失败为并行高负载下既有 Business Order PDF 测试的 5 秒超时，该文件空闲后单独重跑 5/5 通过。Web 全量 1099/1100 通过，唯一失败仍是既有跨月日期敏感停车测试 `pickup cohort remains frozen across later restore and unsubmit lifecycle changes`。

3220 已由 `com.whcarservice.candidate-runtime` 受控重启并恢复运行；监听进程工作目录确认为本候选仓库 `apps/web`。应用内浏览器实测正式检查报告 1 已显示单位、数量、含税单价、本项折扣、小计、三类合计、整单优惠和折后报价，原有 JMD 15,800.00 总额保持不变，控制台无错误。重启前后该检查报告均为工作区 V0、记录版本 1、工作区版本数量 0，验收没有保存或修改业务数据。

## 2026-09-05 办公室联与客户联成套生成和打印

用户确认办公室联、客户联为同一组费用确认文件。生成区现只有“费用确认单（办公室联＋客户联）”和独立维修工联入口；选择中文则两联均中文，选择英文则两联均英文。`customer_copy` 的 `bo-a4-v16-paired` 渲染器使用同一冻结快照，先办公室联后客户联，生成一个 PDF；预览、下载、打印共用同一文件。旧独立办公室联保留在历史展开入口。

Business Order 1 的 CUS-20260905-0001 已追加 R6（revision 41），中英文分别 2 页 A4，四页已视觉检查，发布文件 SHA-256 与审核文件一致。收费、收付款、绩效事实未改动，历史版本保留。审核输出位于 `/Volumes/公司文件/Whole Hearted QA/2026-09-05-document-grouping/paired-v16`。

验证：PDF 与服务集成 34 项、相关组件 16 项、打印消费单元测试 5 项通过；根/Web 类型检查、修改范围 ESLint、生产构建通过。3220 已重启并实页验证 R6、两页翻页、中英文切换、统一生成入口和手机版本/语言入口，浏览器错误和警告日志为空。未执行实体打印机出纸测试。

## 2026-09-05 单据阅读区、JOB 与系统打印实测

- 单据区采用紧凑顶部操作栏、按需展开的历史面板、底部合并翻页与缩放控件；保留 WH 标识和品牌蓝。用 Awesome DESIGN.md 的 Linear 参考整理层级。原有业务标签及生成规则不变。
- 浏览器验证原生高屏、1600×900、767×1024、436×844；全屏、两页切换、历史选择、语言切换和缩放可用。手机 150% 放大只在纸张阅读区产生横向滚动，页面宽度保持 436px。
- 工时固定显示 JOB：收费明细、编辑入口、冻结快照生成、PDF 和旧 HTML 打印视图同步；配件单位及收费数值不变。PDF 渲染器为 `bo-a4-v17-paired-job`。CUS-20260905-0001 追加 R7/revision42（中文、英文各两页）；MEC-20260905-0001 追加 R5/revision43（中文一页）。只追加打印版本，原版本和财务事实保留。发布文件 SHA-256 与 `/Volumes/公司文件/Whole Hearted QA/2026-09-05-document-grouping/job-v17` 的审核 PDF 一致。
- **系统打印尚未完成实际浏览器验收。** 旧实现 `window.open(blob:)` 等待 load 后立即关闭窗口，在内置浏览器实测超时。已改为对当前冻结 PDF 的全部页面渲染 300 DPI 打印专用画布，在主窗口调用原生打印；保留页面至 afterprint，未收到 beforeprint 时明确报错，不报告打印成功。准备时有进度状态和重复点击保护。失败区的“重试打印”真正重新调用打印，并提供当前语言/版本 PDF 下载按钮。
- 同源 iframe 和最终主窗口调用在当前内置浏览器均未启动原生打印。最终版本实测中文两联点击后进入明确失败提示，页面与标题恢复；不能声称 Safari/Chrome 已通过，也未实体出纸。下一步应在用户实际使用的原生 Safari/Chrome 验证打印对话框、A4 两页、取消后恢复及下载路径；不要用 mocked print 测试替代此验收。
- 验证：PDF/服务集成 37 项、组件 38 项（含实际打印面 DOM、双页 300 DPI、取消清理及错误重试）、打印/预览单元 9 项通过；Web 类型、修改范围 ESLint、生产构建、git diff --check 通过。3220 托管进程已重启。后端全量测试本轮未运行。

### 系统打印验收更新

用户随后明确确认：“可以打印了 没问题了”。打印按用户实际操作确认通过，停止继续修改打印实现。此前自动化内置浏览器未触发原生打印事件的结果仅是该验收通道的局限，不再作为业务系统打印失败的结论。未另行验证实体打印机出纸，不扩展用户确认范围。

## 2026-09-05 双联连续预览与缩放定位

费用确认单的成套 PDF 现在同时渲染办公室联、客户联，上下连续滚动；顶部显示总页数和两个直达入口。仅 paired 文件启用此模式，其他单据保留原预览。手机入口点击高度 44px。PDF 文件、生成规则、业务数据和已获用户确认的打印实现均未更改。

实页验收发现缩放后页码为第二页但画面回到第一页，新增失败回归测试后修复为重绘后保持当前联次。最终 40 项组件测试、6 项打印消费/画布单元测试、范围 ESLint、生产构建及 git diff --check 通过。3220 托管服务已重启并实测原生高屏、1600×900、436×844；中英文均同时存在两个页面，直达入口可用，150% 放大和窗口缩窄后仍定位客户联，手机页面没有横向溢出，浏览器错误/警告为空。

本次使用既有 CUS-20260905-0001 R7/revision42 验收，未追加单据版本。实页当前收费已是 V21，该文件仍为 V20，系统正常提示历史版本；本次没有改动收费或财务记录。已恢复桌面尺寸与中文选择，随后用户切至检查结果列表，未抢回页面。

## 2026-09-05 夜间核心路径持续修复

用户授权设定目标并持续执行，不等待普通确认。当前 goal 和每 30 分钟接续 heartbeat `whole-hearted` 已启用，范围为 3220 业务单、单据、检查结果的响应式交互与错误恢复。详细进度、证据和待办保存在 `/Volumes/公司文件/Whole Hearted QA/2026-09-05-core-paths/progress.md`；继续任务前先读该文件，避免重复检查或把进行中事项当作完成。

首项检查结果列表已部署：手机单列 / iPad 双列信息卡片，桌面保留表格，报告编号和整卡均为键盘可用的真实链接。436×844、767×1024、1600×900 实页通过。详情页头已改为全宽身份区，手机编号不再挤成三行，移除嵌套 main；同三种尺寸实页通过，状态更正入口可打开/关闭且未提交。最终 43 项组件回归、构建、详情文件 ESLint、git diff --check 通过；列表文件既有 effect lint 问题仍待后续请求状态修复。手机页头还可进一步精简，英文空结论与工时单位不一致已列入下一步。没有修改真实业务数据或打印实现。

### 04:16 检查报告恢复路径更新

空英文结论已有待补提示和编辑入口；检查报价及中英文报告工时统一 JOB。重试成功会清除旧读取错误；跟进刷新不再覆盖未保存报告草稿，不同报告隔离草稿；草稿使用其原始 expectedVersion 保留并发保护。回复登记成功后修复 await 后 event.currentTarget 失效导致的假失败。50 项组件测试、构建、详情文件 ESLint、git diff --check 通过；三尺寸实页和中英文 JOB 检查通过，未提交真实业务记录。并发冲突的完整核对/恢复界面仍待实现，另有列表查询返回、手机紧凑度、业务单操作区审查待推进；目标仍 active。

### 04:24 版本冲突恢复已部署

“核对最新版本”只读对照本地草稿与最新记录；核对后保留本地或采用最新均仅载入编辑区，仍需主动保存，服务器 expectedVersion 并发校验未绕过。关闭保留原草稿，读取失败可重试。53 项组件测试、生产构建、详情 ESLint 和 diff 检查通过；3220 PID 60585 cwd 已核实。原生高屏、1600×900、767×1024、436×844 实页窗口与操作可见、无页面溢出，截图 12–15；未在真实记录上模拟冲突或保存数据。长内容滚动到底的交互仍待补验，详细证据与下一步见外置进度文档。目标和 whole-hearted heartbeat 继续有效。

### 04:29 检查列表返回条件恢复

实页确认详情返回会丢失搜索条件，已修为三种报告入口带列表搜索/页码上下文，返回重建本地列表查询。异常页码不进入返回链接。56 项组件测试、生产构建、详情 ESLint、diff 检查通过；列表既有 effect lint 问题仍待处理。3220 PID 60956；桌面、平板、手机均实测车牌筛选往返保留，未改真实业务数据。下一步继续列表请求/重试和 URL 输入同步、业务单移动端；外置 progress.md 为详细进度来源，目标保持 active。

### 04:36 查询状态与 BO 数量更新

列表导航清除搜索时旧输入残留已修复并实页复验；请求结果按查询 key 匹配，加载/失败不冒用旧数量，重试成功清除错误，晚到响应不覆盖当前查询。列表两项既有 lint 问题已解决。BO 明细工时/配件数量移除 .000 等全零小数尾，非零历史小数不擅自取整。最终 61 项组件测试、构建、三个修改组件 ESLint、diff 检查通过；3220 PID 61487，三尺寸整数数量与手机收付款定位/历史展开通过，未写入业务事实。后续继续 BO 编辑恢复、空问题描述、快速输入/中文输入法与弹窗滚动；详细证据见外置 progress.md，目标 active。

### 04:47 收费草稿保护及版本核对

收费保存原先固定传 0 清除分类/整单优惠，已修为保留草稿开始时优惠；并发基线不会随刷新漂移，读取重试成功清旧错误。新增“核对最新收费”，人工比对双方项目/备注/所有优惠后，仅载入草稿并选定版本，仍需保存；关闭、失败重试、作废/权限保护有回归。全组件 67/67、范围 ESLint、生产构建、diff 检查通过。BO 编辑器三尺寸无横向溢出、取消/标签草稿保留实测通过；新核对弹窗部署后实页验收正在进行，最终证据见外置 progress.md。未修改真实收费、绩效、收付款和已接受的打印实现。目标 active，继续移动端长表单与错误恢复。

04:50 补充：PID 62283 候选 apps/web；核对弹窗三尺寸实页、关闭及取消编辑通过，截图 23/24，无控制台错误/警告。未提交真实记录。长内容到底、手机编辑表单底部操作、核对关闭触控尺寸仍待继续；详细证据与限制见外置 progress.md。

### 04:55 长收费表单操作完成一轮修复

末尾新增放弃、核对、保存收费与备注；保存期间禁用编辑和取消，同步 in-flight 防重复，失败保留输入。69 项组件测试、范围 ESLint、构建、diff 检查通过。3220 PID 62596；436×844、767×1024、1600×900 末尾44px按钮与退出实测通过，收费核对真实滚动至最后备注且未被固定按钮遮挡（截图25/26）。未保存真实收费，浏览器恢复桌面。浏览器 AX 通道超时，恢复后 Playwright 正常，后续复用 browser/whTab，不重复选浏览器。继续空问题描述/历史入口、AI翻译与保存并发、检查列表输入专项；完整待办与验收限制见外置 progress.md。目标保持 active。

### 05:02 空问题描述与人工优先翻译

空描述显示待补并保留历史入口，含只读用户；英文历史缺翻译时保留中文原证据。项目/备注AI翻译不覆盖等待期间人工修改，取消或重新编辑后忽略晚到结果；顶部和底部可停止翻译恢复保存，失败可重试/手填。75组件测试、范围ESLint、构建、diff检查通过。PID63129候选apps/web；问题描述三尺寸、历史展开/收起、补充窗口打开取消实测通过，截图27。未执行真实AI请求或任何业务保存，AI异步保护为隔离测试证据。继续自然语言AI整理取消/退出/保存竞态及窄屏提示验证、检查列表输入专项等；完整未完成项见外置progress.md。目标active。

### 05:07 自然语言整理取消/退出恢复

请求序号保护取消、收起、人工重开和退出后的旧AI结果；异常与空结果在输入区显示，原文保留重试。已有编辑期间可停止整理后保存，底部有停止入口。80组件测试、范围ESLint、构建、diff通过；PID63540候选apps/web，三尺寸静态输入/收起恢复/取消实测通过，截图28。未真实请求AI或保存业务。下一项重点：stageNaturalLanguage目前忽略pendingQuote且null单价转0，需沿API/存储/展示核实并区分待报价与免费；不能擅自改真实金额或新增业务硬门槛。完整待办见外置progress.md，goal active。

### 05:20 待报价存储修复中，尚未上线

已补 formal charge pendingQuote 字段、计算/读写、单据/收据/交单快照及0051迁移；未定价可用空单价+pendingQuote保存，不作为免费。补价新增版本且原状态不变，历史0元不推断，旧金额迁移不变。交单精确JSON校验已兼容旧已定价格式并验证新待报价标记，没有更改绩效规则。

89项后端测试（88项一组+1项迁移守恒）、80组件测试、范围ESLint/diff、独立目录生产构建通过。重负载并发构建时1条PDF测试超时，错开并发后全88通过，未放宽超时。当前3220仍PID63540，未重启/未执行真实迁移/未写真实数据。SQL0051刻意尚未登记journal，**不要直接重启或部署现在的半条链路**。

下一步立即接通AI/本地解析→草稿pending字段→编辑/核对/只读收费→合计待补提示→PDF/content/HTML打印，再登记0051、构建、托管重启与三尺寸验收。对应计划 `docs/superpowers/plans/2026-09-05-charge-pending-quote-storage.md`。注意本轮较早一次默认build已写.next；后续验证使用NEXT_DIST_DIR=.next-pending-quote-verification避免再覆盖运行产物，最终部署仍需正式完整build。自动生成tsconfig/next-env额外目录引用已还原；独立构建产物215MB在外置卷。根目标仍active，不等待用户。

### 05:47 待报价全链路已部署，旧金额守恒

本节取代05:20的“不能重启/待接通”状态。AI decoder、正式本地回退、收费草稿/保存/并发核对、只读类别与总计、列表待报价数量、PDF与收据HTML打印均已保留pendingQuote；明确免费0元与未报价空白分开。人工暂勾待报价再取消，原输入价格和折扣恢复；正式保存待报价贡献0但保留标记，补价追加版本。已有报价部分收齐但仍有待报价时，不显示“财务已结清”。不修改绩效计算。

0051已登记journal并实际应用到55433。只读校验脚本 `scripts/qa/audit-pending-quote-migration.ts before|after` 核对24张业务表，迁移前后及实页取消测试后行数/逐行内容哈希全部相同（仅排除新pending_quote字段）；历史待报价数0。真实收费、支付、退款、绩效均未写入。最终默认生产build与托管重启完成，3220 PID65581，候选apps/web。tsconfig/next-env临时构建引用已还原。

验证：后端11文件120项、前端15文件86项、解析/打印纯单元25项全部通过；范围ESLint与git diff --check通过。按PDF skill调用真实渲染器生成外置隔离中英文PDF，各2页，4页PNG逐页目视通过；renderer `bo-a4-v18-pending-quote`。实页电脑1600×900、平板844×1127及698×931、手机436×844无横向溢出，勾选/取消及恢复5500单价与700折扣实测通过；手机底部放弃/核对/保存均44px且可见。截图29–34，PDF及数据哈希在外置QA目录。一次尺寸切换后的点击未落到目标，重读DOM再次点击正常；不是已确认产品缺陷。

现有费用确认单V20/R7仍显示办公室联+客户联两页，未生成新业务单据；收费仍V21，应收59200、已收24700、退款10000、未结44500，本轮绩效−500。日志无error/warn；浏览器已恢复原生尺寸，当前documents标签，所有测试草稿放弃。

下一轮直接继续：数量输入仍显示2.000等，需只去冗余零并按用户整数QTY规则处理新输入，不四舍五入历史非零小数；跨业务单切换隔离未保存草稿和AI晚到响应；检查报告核对末尾、列表输入法/快速输入及手机关闭触控。wholeOrderDiscount后端强制0是既有规则待查依据，不可凭前端保留测试宣称已端到端保留。根目标active，继续执行，不等用户回复。

### 05:56 串单隔离与整数数量完成部署

- BusinessOrderDetail按businessOrderId使用key隔离全部草稿、弹窗和异步状态。新增两个真实组件回归，先复现切单后旧草稿/晚到AI污染新单，再验证修复。同一业务单换标签仍保留编辑，实页已验证。跨单晚到响应验证来自组件测试，不宣称在真实AI/多单生产数据上测试。
- 新增BusinessChargeQuantityField，编辑器将2.000显示2，非零历史小数1.250保持原文，明确提示核对数量与单价。输入整数去冗余零；新保存数量须为正整数。保存遇到错误会直接聚焦并滚动到错误格，人工修正后可继续。后端同规则接受2/2.000并标准化，拒绝小数，不追加版本、不改历史。正式AI decoder拒绝小数及非安全整数，不取整。
- TDD：数量显示/历史保留/错误定位、后端schema、AI小数和过大数先红再绿。完整组件15文件89项；后端schema+隔离service集成2文件18项；AI与本地解析纯unit21项通过。范围lint和diff通过。构建曾发现测试误用testing-library不支持的exact参数，已移除，最终生产build成功。
- 3220最终托管PID66558，候选apps/web；不涉及新迁移。桌面2005×1575、手机436×844、平板767×1024实页无横向溢出，错误提示无重叠；截图35–37在外置QA目录。实页输入1.25后点击底部保存，焦点回数量格且收费仍V21，全部测试草稿随后放弃。输入2.000失焦变2；同单换标签仍保留1.25。控制台无error/warn。
- 真实24表哈希最后校验仍factsUnchanged=true，historicalPendingCount=0。一次托管重启后立即校验遇PG启动中ECONNREFUSED，待55433监听后再校验通过；不是数据错误。未改真实费用、绩效或删除记录，已恢复原生尺寸和业务单明细页。
- 下一步：检查报告核对窗口长内容末尾和手机关闭触控；检查列表IME/快速输入/异常页码；wholeOrderDiscount后端强制0仍待查规则依据，不擅自修改。目标active，本轮仅进展，不等待用户回复。

### 06:03 检查报告弹窗可达性与失败恢复

- 实页发现更正状态自制弹窗未锁定背景、焦点仍在后方触发按钮；844×320横屏表单top−8.86、bottom328.88超出屏幕，无内部滚动。失败提示写在全页弹窗之后，不能在弹窗内看到。核对版本窗口长内容可正常到末尾，非缺陷；但共享关闭按钮实测36px。
- 更正状态改用现有Dialog portal/焦点隔离/滚动/关闭恢复，手机全屏、桌面max-w-lg，按钮44px。新增局部错误区，失败保留状态和原因并提示再次保存；处理空白原因，增加请求进行中保护，保存时不可重复提交或关掉未完成请求。共享Dialog关闭按钮36→44px，不改报告/跟进的业务语义。
- TDD新增2条：错误必须在dialog内且保留选择供重试；Escape取消不得写入、背景滚动恢复。先红后绿，完整组件15文件91项通过。范围ESLint通过（渲染中错误读取ref已改为busy状态；既有portal创建后同步host的effect加单行解释性lint例外，保留外部DOM同步结构），生产构建成功，git diff --check通过。
- 3220托管重启PID66957，候选apps/web；真实24表哈希仍完全相同。未真实提交状态更正、报告版本、付费AI或客户通知。失败重试与Escape由真实组件/隔离API mock回归，不谎称制造了生产网络错误。
- 实页桌面1600×900、平板767×1024、手机436×844、横屏844×320无横向溢出。横屏新dialog top15.99/bottom303.99，clientHeight286/scrollHeight430可滚动，底部按钮236–280在视口，全部44px。手机核对最新版末尾可见报价总额15,800且选择按钮不挡内容，关闭焦点正常。截图38–43存外置QA目录，40/41/42为主要修复后证据。控制台无error/warn，恢复原生尺寸，当前IR1，所有弹窗关闭。
- 下一轮直接查检查列表IME/快速输入/异常页码；记录下一步定位：列表文件不叫formal-inspection-report-list.tsx，需从页面import查找。wholeOrderDiscount后端强制0仍待核对业务决策来源。根目标active，不重复建自动任务，不等用户回复。

### 06:08 检查列表中文输入与页码恢复

- 真正正式列表为`formal-inspection-reports-workspace.tsx`，不要改Mock的`inspection-reports-workspace.tsx`。实页page999实际显示第1页记录，却把listPage999带进详情；Infinity同样传进API/返回链接。另通过组件事件复现IME中间拼音立即路由、输入回退。
- 现在仅正安全整数作为请求页码；按服务器返回page显示页码、计算上一/下一页及详情链接，并纠正URL保留search。第1页省略page。中文composition期间本地显示输入，不路由；选字结束后才提交搜索。queryString稳定依赖避免每次render重取数据；保留既有旧响应隔离/失败重试。分页按钮44px。
- 新增7条先红后绿（Infinity/1.5/负数/字母/超安全数、服务器页码回落、IME事件）；既有page3测试fixture原返回page1不真实，改成page3/pageCount3/total41再验回链。完整组件15文件98项通过，范围lint、生产build、diff通过。
- 已部署3220 PID67333。实页Infinity刷新回规范列表；search4321AB&page999回search4321AB，详情与返回不再带999。不存在查询显示0，快速43→432→4321AB最后URL/输入/卡片一致；手机进入报告返回保留条件。自动化fill('')没有删除原文，用真实键盘ControlOrMeta+A/Backspace验证清空成功，非应用缺陷。IME系统候选窗未实测，组合事件回归通过，不夸大成真实系统输入法验证。
- 桌面1600×900、平板767×1024、手机436×844无横向溢出。截图44修复前无效页码、45手机搜索结果。控制台无error/warn，24表哈希未变，恢复原生尺寸并停未筛选检查列表。
- 下一轮：实页查新建检查结果弹窗焦点/关闭/输入保留（源文件仍自制overlay/h9关闭，先复现再修），补三条核心路径验收清单。整单折扣证据冲突已定位：未批准基线120行说只本项折扣，而旧救援规格153行及路线图100行说支持整单；现service711强制0，测试明确期待忽略1500。不能用未批准基线裁定业务，未改金额算法；可继续核实git历史或独立安全工作，不把该问题当全目标已解决。


### 06:16 新建检查结果的加载恢复

- `formal-inspection-create-dialog.tsx` 原班组加载失败只留错误，无重试；车辆搜索250ms等待阶段会提前显示没有匹配/新建车辆，失败也误作空结果；旧请求即使取消仍可能覆盖新结果。新增3条组件回归先红后绿确认。
- 班组加载有明确等待/失败/原地重试，暂无启用班组有维护提示和重新加载；重试保留已填结论。车辆结果绑定规范车牌与重试序号，等待和失败不显示新建入口，只有成功空结果才显示；失败可按原车牌重试，取消的旧响应不再写入。未改创建数据契约或后端业务规则。
- 全组件15文件101项、范围eslint、生产build、diff检查通过。3220已部署PID67762。重启瞬间旧tab4进入连接拒绝data错误页，Browser策略禁止该错误页操作；在同一个已连接Browser新建正常HTTP标签恢复，whTab现绑定whRecoveryTab，不要继续使用旧tab4。无换浏览器或读取受限错误页。
- 实页加载提示、4321AB选择、车间一组、不指定维修工、检查草稿、更换车辆、ZZQA999成功空结果均验证；更换车辆仍保留结论和备注。桌面1600×900、平板768×1024、手机480×928无横向溢出，手机底部取消/创建可达（833–873），截图46-inspection-create-phone.png。最后取消测试草稿，恢复原生尺寸，停检查列表，日志为空。
- 网络失败和迟到响应为组件API mock验证，不是生产断网测试。没有创建任何真实车辆/检查记录；最后24表哈希factsUnchanged=true、historicalPendingCount=0。
- 仍待检查：新建IR自制弹窗焦点隔离/Escape/36px关闭和保存中退出；嵌套VehicleFormDialog需一起验证，不能盲目替换Dialog。当前并未修这个弹窗的焦点机制。整单折扣规则冲突仍保留，三核心路径完整验收矩阵未结束。根目标active，本轮PROGRESS，不重复建自动任务。


### 06:24 新建检查结果退出闭环与嵌套焦点

- 上轮属于PROGRESS：真实代码、测试、部署有变化。本轮继续从实页复现：旧创建窗口无背景inert/锁滚动，Escape无响应，取消直接丢输入。已换共享Dialog，手机全屏、桌面紧凑内部滚动；关闭/底部/确认按钮44px，脏草稿有继续编辑与放弃路径，Escape可返回编辑，空草稿直接关闭。
- 创建以ref阻止重复提交，等待时锁定字段保留原文。接口失败恢复可编辑及原地重试。接口自身无超时，因此关闭按钮/Escape在请求中给出“继续等待／关闭窗口，稍后核对”，明确关闭不撤销服务器保存，避免无限等待的死路；不加虚假取消或自动再次提交。实际保存仍只在mock测试执行。
- 新增4条创建窗口先红后绿回归：背景隔离/空窗Escape、脏草稿保留/放弃、重复提交/失败恢复、请求中明确退出且不重发。构建抓到testing-library不支持exact参数，已删该测试参数而非跳过类型检查。
- 嵌套车辆实页另查到：启动按钮加载时失焦，子Dialog关闭把焦点还给BODY。新增dialog-focus-recovery.test.tsx先红后绿，shared Dialog在原焦点丢失/无法聚焦时回到最上层仍有效的Dialog。保留正常原触发器恢复。实页二次验证activeTag BUTTON/activeLabel关闭/insideDialog true，子窗没有关闭父窗，草稿保留。
- 全组件16文件106项通过，最后两相关文件10项再验通过，范围eslint、最终生产build、diff通过。已部署PID68509（candidate/apps/web，3220）；重启前后确认listener再reload，本轮无断连错误页。
- 桌面1600×900、平板768×1024、手机480×844、横屏844×320无横向溢出。手机放弃提示三个按钮均44px，截图47；横屏Dialog top16/bottom304、client286/scroll1025可滚动，底部取消/创建完整可见，截图48。真实打开车辆子表单后Escape退出，未创建车辆、检查记录或调用AI。最终24表哈希未变，日志空，恢复原生尺寸和检查列表。
- 下一步做三核心路径完整验收矩阵，不把106个组件测试当全系统完成。特别核对：请求中退出后晚到onCreated回调是否影响后来新开的窗口；创建成功回调异常不可误提示保存失败；三条路径的失败/重试/取消/返回/过期版本/语言与打印证据。整单折扣规则依据冲突保持原样，不擅改真实金融规则。目标active，本轮PROGRESS。

### 06:32 创建完成竞态与验收矩阵

- 新建检查请求退出后，晚到的 onCreated 过去会关闭后来打开的新草稿，业务单入口还会跳走。现在标记 background 完成：列表仅刷新；业务单仅刷新关联结果；不关闭后来窗口、不跳转。已保存报告与回调/跳转异常分开处理，显示真实报告编号和打开链接，不误报“创建失败”或允许再提交。
- create-continuity 与 list-access 新增两条回归及一条原测试扩展，先红后绿；全组件16文件108项、范围ESLint、生产build通过。核心后端/PDF/检查/交单5文件73项再次通过；ir-pdf-print 3项通过，diff检查通过。
- 已部署3220 PID68933，cwd确认候选apps/web。列表新建输入后取消/放弃、业务单来源新建空窗取消实测正常；没有真实创建。竞态和回调异常来自真实组件+模拟API，不宣称生产制造了延迟/故障。日志空，原生2560×1440无横向溢出，浏览器停BO1 operations无弹窗。
- 24张业务表最终哈希仍factsUnchanged=true，historicalPendingCount=0。没有改真实财务、绩效、检查、车辆、通知数据。
- 已新增 `docs/qa/2026-09-05-core-paths-acceptance.md`：三路径逐项列实页/组件/集成证据与未验收项，不能把累积测试数当全系统完成。下一步优先检查结果删除预检/错误恢复、状态更正保存中无期限请求退出；然后补业务单新建背景完成专门回归、超长双联PDF和手机缩放、绩效/无效轮次只读恢复路线。整单折扣规则冲突未裁定。根目标active，本轮PROGRESS。

### 06:39 删除预检不再卡死或沿用旧资格

- 实页IR1有3条客户沟通，预检正常返回策略阻断，不是先前IR2工作区删除500。不要删除IR1造测试。共享RecordDeleteButton预检失败无重试、busy禁止只读退出、范围改动沿用旧preview、409提示被loadPreview清空等缺陷已通过5条新真实组件回归先红后绿修复。
- 新增preview请求序号与checkedSelection；读取可以退出且旧结果失效；执行删除仍独立ref防重复和禁止假取消；成功预检与当前所选范围一致才允许最终确认，重查失败保持不可提交。始终有44px重查，阻断时可返回，IR增加查看相关记录→客户跟进/历史入口。没有改后端删除政策。
- 全组件17文件113项、删除策略/执行集成2文件14项、范围eslint、生产build、diff通过。已部署PID69385/candidate apps/web；三尺寸1600×900、768×1024、480×844无横溢、按钮44px；实页重查/转客户跟进成功。截图49/50，50目视通过。日志空，24表哈希未变，恢复原生尺寸，当前IR1客户跟进无弹窗。
- 本轮whTab旧绑定不在session；同browser.tabs.new恢复正常HTTP标签，复用browser不重选、不动Lark。下一轮若旧标签消失照此恢复。
- 重要：新增查看入口不等于沟通记录可作废，不能宣布删除闭环完成。继续检查状态更正/正式删除执行中无期限等待与成功导航异常；后者已有requestId去重，不能关闭后生成新id误导重发。再查沟通作废规则依据和后端能力（无需改真实数据），补BO来源背景回调、长篇双联PDF验收。验收矩阵同步更新。根目标active，本轮PROGRESS。

### 06:45 状态更正可后台等待，不再把空响应当成功

- 状态更正等待中现在可以关闭/返回阅读报告，外部持续显示待返回和查看进度；重开恢复同次已提交状态及原因，禁止重复提交。失败保留并可查看原内容/核对客户跟进；成功刷新并通知，清空已提交草稿，不重开弹窗。仍保留同页busy防重复写，不代表取消服务器请求。
- recordFormalInspectionNotification原200响应json失败被吞成{}并return，null和错报告也算成功。新真实函数单元先复现5种异常，现正safeInteger id/报告归属校验失败提示无法确认，必须先查历史；不自动重发、不伪造服务端取消。通信幂等requestId/自动结果核对尚未实现，错误后的人工重试仍需核对。
- 2条组件红绿+6条新增API单元，完整组件17文件115项、API unit7项、范围eslint、生产build/diff通过。托管部署PID69834，cwd候选apps/web；未改后端SQL/数据。三尺寸1600×900、768×1024、480×844实页仅空格本地校验→核对客户历史→查看原内容/关闭；4按钮44px、手机footer621–665、无横溢。截图51已目视。异步等待/成功/失败仅隔离API模拟。
- 最终真实24表hash未变、日志空。reload清掉校验测试状态、恢复原生，当前IR1检查与报价无弹窗。根目标active，本轮PROGRESS。
- 下一步：共享RecordDeleteButton正式execute仍禁止关闭、await后的router异常仍混入删除失败；需保留既有requestId和提交payload，不能重开时另造id，先写模拟测试再改。检查创建API也有json失败吞{}，列为后续真实响应校验。长篇双联PDF/统一预览验收、BO来源后台回调专测仍未完成；沟通事实作废与整单折扣存在政策问题，不能凭UI入口宣布闭环。
