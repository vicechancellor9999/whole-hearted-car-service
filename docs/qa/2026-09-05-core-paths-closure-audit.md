# 三条核心路径收敛审计 · 09-05 15:07

## 结论

目标未完成。不要再用累计测试数量代替整体验收，也不要将下列规则缺口写成“已有入口所以闭环”。最新候选监听 PID92917，源码仍在单体候选目录；正式系统工作树未作为本轮修改对象。

真实浏览器连续三个目标轮次停留在3220登录页。本轮检查所有现存标签，另外两页均为Lark，没有可用的已登录候选页面。最近的隔离布局测试已经结束，临时服务和页面已关闭，没有等待中的后台任务。恢复真实登录是最终实页验收的前置条件；不能注入身份、绕过权限或把隔离fixture冒充正式数据。

## 逐项核对

| 目标要求 | 当前证据 | 完成判断 |
| --- | --- | --- |
| 先实页发现，再修复可复现问题 | 14:53金额换行及小触控目标有真实DOM失败证据；15:03窄容器裁切在真实组件隔离页复现 | 已遵循；后两项仍缺最终3220绿测 |
| 业务单左右独立滚动、区块连续、不整页横溢 | 14:53原生1337×1050、1600×900、768×1024、391×843；右栏展开历史gap11.998px，overflow:auto | 该布局正常路径实页已验证 |
| 工时/配件/其他区分，名称描述分开，分类及总额 | 当前BusinessChargeSection、金额行浏览器断言；本轮calculation测试通过 | 已有证据；极端长度/金额不能仅凭当前小样本声称全覆盖 |
| JOB、整数新QTY、历史非零小数不擅自取整 | 当前组件与已有schema/AI/PDF回归；document-pdf本轮重跑包含JOB和数量格式 | 已有测试及历史实页证据；未修改历史数据 |
| 金额不换行 | verify-charge-money-lines对15金额实页逐个验证单行、列内与页面边界；四尺寸通过 | 已验证本次复现案例 |
| 办公室联与客户联同内容、同语言成套生成 | document-pdf本轮含两联内容等价、纯英文、责任费用边界；14:53当前CUS V21/R1连续两页实页 | 已有证据；长单允许每联自然续页，不硬压成两张 |
| 费用确认不混入付款记录，收退款归收据 | document-print-boundaries组件；本轮PDF测试排除receipt事实 | 已有证据；历史单据未批量重生成 |
| 维修工联本单本轮绩效，不重复继承整单工时 | PDF与formal-handoff隔离服务本轮通过，后者包含独立售后轮次、旧客户端整单值拒绝、调整及留痕 | 隔离执行证据；未改真实绩效造测试 |
| 缩放、翻页、放大可读、三端可操作 | 14:53双联手机150%只在单据内横滚，平板/桌面翻页；已有预览组件异常测试 | 已验证所测正常路径；最终登录回归仍需补 |
| 系统打印与下载 | 已有真实PDF生成器/打印函数隔离测试，用户曾确认系统打印可用；长责任三语PDF及11张渲染图仍存在 | 本轮没有实体打印或真实下载落盘证据；未验证外置下载目的地前不点击下载 |
| 检查报告独立，不自动改业务单费用/派单 | 本轮inspection-report-service隔离PGlite测试通过，源码检查仍分离 | 已有隔离证据；未创建真实QA报告 |
| 检查编辑三端触控、无裁切 | 15:03真实组件隔离页39控件四尺寸通过，第三项目增删后金额保留；源码采用容器断点 | 隔离通过，3220最终实页待恢复登录 |
| AI等待/停止/重试、人工草稿优先 | 既有组件覆盖迟到、取消、人工输入、跨单；未付费调用 | 不宣称真实供应商质量/计费取消已验收 |
| 保存/删除/绩效失败恢复，关闭/刷新不丢请求身份 | 已有279组件覆盖请求期限、原请求恢复、账号隔离、异常回执、草稿核对；本轮删除服务执行集成重跑通过 | 隔离故障证据，不冒充真实删除或财务写入 |
| 无效记录可处理后删除，而非永久堵死 | 当前policy仍拒绝有正式单据/客户沟通的对象；已有依赖查看/预检重试 | **未完成**：未有获准的相关作废规则与实现 |
| 整单折扣一致 | service第711行仍覆盖wholeOrderDiscount为0；baseline第120行禁止整单折扣，roadmap第100行要求显示整单折扣 | **未完成/规则冲突**：不能只改算法或静默去字段 |
| 不改真实财务/绩效、不删用户记录、不扩展内账采购 | 本轮只读源码/实页登录状态与隔离PGlite测试；无真实API写入；未改业务源码 | 本轮符合边界；未重跑24表哈希，不声称所有外部操作者均未改数据 |
| 所有产出外置、交接可续接 | QA脚本/截图/fixture/缓存均在外置卷；候选交接入口、验收矩阵、progress记录 | 已保存；临时测试服务已停止 |

## 本轮最新执行证据

已检查所选集成测试使用 `new PGlite()` 内存隔离库，不连接候选PostgreSQL。执行：

```sh
pnpm exec vitest run src/modules/business-order/business-order-calculation.test.ts src/modules/business-order/business-order-document-pdf.test.ts src/modules/business-order/formal-handoff-service.integration.test.ts src/modules/inspection-report/inspection-report-service.integration.test.ts src/modules/record-deletion/record-deletion-policy.test.ts src/modules/record-deletion/record-deletion-execute.integration.test.ts --maxWorkers=2
```

结果：6文件93项通过，exit0，20.23秒。没有新增产品修复；本轮进展是当前证据复核与剩余项收敛。15:03的279组件、eslint/build结果属于上一轮，未伪称本轮又跑过。

## 恢复工作顺序

1. 用户正常登录3220后，对两项检查编辑CSS修复运行实际页面三尺寸回归；保留手机/平板/桌面证据。不要再次实现已完成的刷新恢复或金额换行。
2. 明确正式单据/客户沟通的作废资格、审计留存、对已发送副本的处理，才能补依赖解除闭环。现有“查看记录”不是作废功能。
3. 明确业务单折扣以仅逐项还是兼容整单为准；两份现有规范冲突，当前服务忽略整单输入。不得静默裁定或改真实账。
4. 确认受支持的外置下载路径后验证最终文件。保持用户已确认的办公室/客户成套、语言分开、JOB、分类合计、维修工绩效规则。

维修轮次专用只读回执查询是可选增强；已有原请求幂等重试和最新历史核对，不把这个新增强伪装成用户原定验收门槛来无限延长任务。
