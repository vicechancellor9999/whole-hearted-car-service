# 三条核心路径验收矩阵

## 当前结论 · 09-05 15:10

目标仍未完成。最新逐项判断见 [收敛审计](2026-09-05-core-paths-closure-audit.md)，程序员接手见 [审查入口](2026-09-05-programmer-review-entrypoint.md)。3220仍为登录页，最新检查编辑修复只有隔离绿测；规则冲突、依赖作废及最终文件验收不能由历史测试总数代替。本轮没有新增产品代码改动或宣称重新执行全部测试。

## 最新候选检查点：09-05 15:03（优先于下方历史检查点）

- 上轮PROGRESS，本轮PROGRESS。真实3220仍为/login（连续第二目标轮出现同一登录阻断）；没有绕过会话或查找真实凭据。使用外置QA/editor-harness搭隔离Vite页面，直接导入真实检查组件/样式，虚构fixture，所有写请求与其他fetch返回403，无后端代理，不是3220生产验收。
- 隔离浏览器发现新窄容器缺陷：698×931时视口sm断点强制6列，删除按钮right658超出报价卡片right639。既有verify-inspection-editor-targets红测确认两处contained=false；改inspection-editor.module.css报价容器查询680px，窄时2列、宽时6列，删除按钮对齐。正式组件只挂容器/数字区class，无业务规则变化。
- 隔离绿测698×931、391×843、768×1024、1600×900，各39控件达到44px/未裁切/页面无横溢。前端新增第三项→52控件通过→删除刚新增项→两项恢复，总额15800不变。截图93为发现时视口（按钮当时在可见范围之外，不充当目视裁切证据）；94手机、95原生修复结果已目视。DOM边界是红测证据。控制台error/warn为空。
- 范围eslint、28文件279组件、生产build/tsc、diff通过；受控部署PID92917，cwd候选apps/web确认。未真实报告保存、财务、绩效、删除、客户通知或付费AI；未重新计算24表哈希。真实登录后的触控/容器绿测仍缺失，不能由隔离页替代。
- 测试启动说明在QA/editor-harness/README.md。隔离服务session97958已主动终止exit130，临时浏览器页已关闭，viewport reset；没有待轮询进程。不要把该服务当持续运行任务。
- 下一步收敛三条路径完成度矩阵：核对当前需求/已修证据/确实未处理缺陷，优先可独立完成的检查；真实登录恢复前保持实页验收未完成。登录阻断本轮仍有实际可做工作，未达真正无进展困境，不标blocked/complete，不新建目标。

## 最新候选检查点：09-05 14:53（优先于下方历史检查点）

- 本轮 PROGRESS：整体实页巡检发现收费卡片在 660px 容器即强制名称/4数字并排，金额列约95px，JMD与数字两行。先新增外置QA脚本 verify-charge-money-lines.mjs，真实DOM Range行数红测；修 business-charge-section.module.css：金额nowrap，容器560px起4列、880px起名称与数值并排，数值列保留内容宽度。
- PID92237实页绿测：原生1337×1050、1600×900、768×1024、391×843，15个金额均1行且在列内，页面无横溢。右栏展开历史后独立overflow:auto，clientHeight763/scrollHeight1395，区块间距11.998px。截图90为原生修复后；91平板、92手机客户联150%，已目视。viewport截图额外画布不当成应用空白。
- 双联只读复核：CUS-20260905-0002 V21/R1仍连续2页，办公室/客户跳转、中文/英文文件href切换正常。手机150%下画布892px、阅读区344/scroll893、页面391，无外部撑宽。iPad/桌面翻页正常。未生成新正式单据，未点击下载或打印。
- 第二个实页发现：检查报价手机删除按钮约15px高、分类32px、常用编辑动作36px。verify-inspection-editor-targets.mjs在平板红测确认多个小于44px。新增inspection-editor.module.css并挂到检查与报价工作区，按钮/非radio输入至少44px、textarea80px、删除图标居中；不改内容或保存逻辑。
- 最终范围eslint、28文件279组件、生产build/tsc、diff通过，受控部署PID92512且cwd候选apps/web确认。随后实页进入/login，登录上下文已不可用（原因未查明，不猜测过期或人为退出），error/warn为空，视口已reset。第二处触控修复尚未实页绿测；不能将第一处绿测移用于它。无真实财务/绩效/删除/报告保存/通知/付费AI写入，未重做24表哈希。
- 下一步：在隔离前端测试环境验证触控修复/窄容器，不绕过真实登录；真实会话恢复后再运行两份QA脚本并复核三个尺寸。其余已确认规则冲突与下载目的地限制保持。此为首次遇到当前登录阻断，本轮仍有有效进展，目标active，不标全范围完成、不重复新建目标。

## 最新候选检查点：09-05 14:43（优先于下方历史检查点）

- 上轮PROGRESS；本轮PROGRESS。轮次删除提交前保存按账号/业务单隔离的原请求、原轮次ID/编号、原因与版本；sessionStorage当前标签页24小时，刷新pending转待核对，不自动POST。即使当前轮次已变化，仍显示原请求恢复入口，不把重试改成删除新轮次。只暂存请求提示，不作为权限或删除事实。
- 新round-deletion-storage.ts校验结构/身份/时间/体积；不能用不同payload覆盖原请求，晚回包不复活已移除提示。新RoundDeletionAttemptView呈现原对象/原因/请求号，人工原请求重试、重新读取服务器历史、关闭、核对后移除。暂存读取失败有重读；写入/清理失败保持提醒并可重试。
- 初始刷新恢复红测缺入口；错误回执红测发现仅cancelled=true而原轮次仍在时会误报成功，补原ID与工作区验证。读历史红测发现直接打开旧快照，改先GET最新维修记录，15秒现有期限，失败留在恢复卡；关闭/账号切换忽略旧读结果。旧账号迟到删除结果仅更新原账号原请求，不改新账号UI。
- 28文件279组件、范围eslint、生产build/tsc、diff通过。新测试含跨刷新当前轮变化、账号迟到回包、错轮次/原轮仍在、暂存清理失败、过期/坏数据、最新历史失败。故障与删除执行全为隔离组件/网络替身，未对真实记录提交删除。
- 最终受控上线3220 PID91862，cwd候选apps/web确认；最终实页预检/重新检查/关闭正常，error/warn为空。三尺寸正常预检391×844（窗口358.67×812，动作44px）、768×1024、1600×900（窗口576×792）无页面横溢；最后reset视口。本轮没有伪造真实暂存来展示故障卡，也没有故障卡的真实浏览器截图，不冒称全部恢复UI实页目视已验收。无真实收费、收付款、绩效、报告创建、客户通知或付费AI修改；未重做24表哈希。
- 下一步回到三条路径统一视觉验收与收敛矩阵，勿重复做已修的轮次刷新恢复。当前恢复只有本标签24小时；核对历史不是只读删除回执查询，后端虽保存repair_round回执，尚无专用只读结果消费入口，不称已具备。正式单据/沟通事实作废规则与整单折扣冲突仍不自行裁定，下载外置目的地未验证前不落盘。目标active，全范围完成未获证明。

## 最新候选检查点：09-05 14:29（优先于下方历史检查点）

- 本轮 PROGRESS：只读实页核对R3删除依赖、重新检查、交单历史及正式单据导航。391×844、768×1024、1600×900无页面横溢，手机动作均44px；截图88已落外置QA并目视，视口已reset。现有正式单据阻断未解除，未执行任何真实删除。
- 找到与共享检查结果删除不同的遗漏：delete_invalid_after_sales提交没有等待上限。两个API红测在30秒仍pending；补30秒期限覆盖头/JSON正文，超时提示结果未确认，可原窗口原请求重试或关闭核对，迟到结果不假报成功、不自动重发。正式维修记录读取原已有15秒期限，不重复修改。
- 增加真实BO组件+真实API适配器测试，只有网络替身：超时释放按钮、保留原因/确认编号、人工重试payload与requestId完全一致、可关闭。首次断言早于微任务发出请求，补flush；首次build因测试库不支持exact选项失败，改名称正则后build/tsc通过。
- 全组件27文件268项通过；最终两个相关文件59项重跑通过；范围eslint、生产build、git diff --check通过。受控部署3220 PID90914并确认cwd候选apps/web，更新后实页只读复核/关闭正常、error/warn为空。未改后台或真实收费/收付款/绩效/检查/通知；不是实际删除成功的验收。
- 已合并验收矩阵里的陈旧待办：长双联、绩效恢复、检查创建和共享删除恢复不再反复列为未做。历史检查点保留，06:32顺序明确归档。
- 真正下一步：维修轮次专用删除窗口仍仅本页保留请求，关闭/刷新会丢原请求身份；与已完善的共享RecordDelete恢复不是同一路径，需隔离测试评估安全恢复。随后统一三路径视觉证据。正式单据/沟通事实的作废规则及整单折扣资料冲突不自行裁定；下载目标未确认外置前不点击落盘。目标active，不另建目标/自动任务。

## 最新候选检查点：09-05 14:19（优先于下方旧检查点）

- 本轮 PROGRESS：真实手机391×844两次复现 ChoiceCards 原生radio点击误选相邻语言。DOM确认 sr-only 控件只有1×1px，y比所属48px高卡片偏后64.33px，三个控件都在所属label之外；不是语言值存储或PDF缓存问题。可见label中心命中正常。未将一次工具超时直接当产品bug。
- 修复 shared/choice-cards.tsx：label relative，原生input absolute/inset-0/完整宽高/opacity-0，保持native radio、disabled、required、name/value、默认值、peer checked/focus样式。hover改由卡片group触发。无新状态库、无业务规则或打印实现改动。
- 实页回归脚本：QA目录 verify-choice-card-targets.mjs，通过现有Browser运行，只做语言UI切换与DOM几何断言，不发业务写入。旧PID87721明确红测（三个inside=false）；新PID90254下391×844、768×1024、1600×900全部inside=true，目标85.33×48，zh→en→bilingual→en→zh逐项通过。英文生成EN.pdf，切BI立即撤旧下载，再生成BI.pdf 1页；日志error/warn为空。
- 27文件265组件、范围eslint、生产build（含tsc）、diff通过。3220受控更新PID90254，cwd候选apps/web验证；没有向3210/正式工作树写入。此CSS布局回归用真实Browser而不是jsdom伪造布局。
- 单据只读复查：现有 CUS-20260905-0002 V21/R1，办公室/客户联连续2页。1600×900与768×1024跳联，391×844在150%下页面scrollWidth=391，单据内scrollWidth893/clientWidth342，工具可达，改回fit。未生成新业务单版本。此前新增版本/快照与页面已生成提示一致，但不归因操作者、不回滚、不重置上午数据基线。
- 截图恢复：本轮实际看到桌面双联与手机放大截图；原生尺寸下检查报告截图已存 87-language-target-after.jpg 并目视。viewport覆盖截图带额外空白画布，不能把它等同应用布局空白；DOM实测与原生截图分开记录。已reset视口。
- 限制/下一步：真实下载落盘和系统打印输出仍未执行，当前Browser仅公开visibility/viewport能力，没有已验证的外置下载目的地控制；不可让文件默认落本机或绕过会话抓取。优先继续业务单明细/轮次依赖恢复只读检查并收敛矩阵里的陈旧待办。语言命中问题已实页闭环，不重复列为未完成。目标active，整体未完成。

## 最新候选检查点：09-05 14:09（优先于下方旧检查点）

- 上轮与本轮均 PROGRESS：删除 preview 加 15 秒、execute 加 30 秒等待期限，覆盖响应头及 JSON 正文；超时释放操作，提交超时显示结果未确认，保留原请求及只读查询/原请求重试。不自动重删，迟到结果不误报成功。新增 4 条 API 挂起测试和 2 条实际组件+真实适配器测试；先红后绿。
- 检查报告 PDF（不是业务单双联生成器）修正中文句号、问叹号、右引号/书名号在满行时单独掉到下一行。原文不截断，金额/数量不变。增加三语 90 行责任说明完整性及签名分页验证；长说明原来已能分页，本轮补证据，不虚称重做。
- PDF 技能：实际生成器三语样本已重渲染，formal-pdf-v3 的中文3页、英文3页、对照5页共11页全部目视检查，页脚/签名/行序无遮挡，标点跟前字换行。都是外置 QA 样本，不是客户交付文件。producer formal-inspection-a4-v3。
- 新测试 filter 联合类型未收窄导致第一次 build/tsc 失败，改 flatMap 明确筛选 TextItem；随后范围 eslint、tsc、生产 build、diff 全通过。27 文件265组件、21前端单元通过，类型修正后15条PDF单元重跑通过。未改后台。
- 11:39 已受控部署 3220 PID87721，候选 apps/web cwd 核实；14:08监听仍同PID。实页IR1中文版/英文版/对照版均从“生成PDF文件”得到对应 ZH/EN/BI 文件名、1页 canvas 预览。语言切换会撤下旧文件。未点击实际下载或打印，不能宣称这两项已端到端验收。
- 上午实测1600×900/768×1024/480×844页面无横溢；切尺寸后立即读到旧canvas是异步绘制，等待后手机canvas382/容器408正常。下午浏览器缩放发生变化，实际视口512与320宽，正文506/314、canvas409/217，无外部横溢，不把请求的viewport参数当实测尺寸。已reset视口。一次手机check中英对照未选中；重新读DOM仍英文，后续新实页正常选中并生成BI，原因未证实，不能归因或称已修复。
- 数据核对：11:39 24表hash不变；14:09相对上午基线仅 business_order_document_revisions 41→42、business_order_document_snapshots 6→7 变化，其余22表相同。本任务此次只生成检查PDF浏览器blob，未调用业务单生成/保存或财务绩效/删除/客户通知/付费AI；不归因新增单据是谁操作，不回滚、不覆盖旧基线。后续先核对新增单据身份/时间再作新的只读验收基线。
- 下一步：真实单据下载/打印输出证据与三条路径整体矩阵；手机语言切换若再次复现，先截图与命中区域确认，再补回归。三条路径全量完成尚未获证明。用户明确续接后再次 get_goal 已确认 active；不另建目标/自动任务、不冒报完成。

更新时间：2026-09-05 14:43，候选 3220 PID 91862。不是全系统完成声明。

代码位置：`/Volumes/公司文件/Whole Hearted Car Service 单体候选/3210-single-runtime`。
连续实页证据：`/Volumes/公司文件/Whole Hearted QA/2026-09-05-core-paths/progress.md` 与同目录截图 01–88。

## 10:52 删除提交跨刷新与公共恢复

- 按已验证账号暂存原删除请求24小时，刷新保留原编号/范围/原因/预览，不自动重发。公共“删除进度”仅在有条目时出现，不依赖原详情，可只读查询和核对列表；提示清理及暂存失败均有本机重试，不伪装为服务器失败。
- 账号变化隔离窗口，generation挡住同tick晚回包导航；结构/账号/过期记录检查，业务事实仍以后端为准。259组件、10前端删除单元、eslint/tsc/build/diff通过，正常三端IR1关联预览无横溢，24表hash不变。恢复场景仅隔离组件验证，无真实删除、无伪造浏览器暂存及新截图。
- 下一步预览/执行请求等待期限（只读查询已有期限），然后PDF长内容/标点/外置下载及整体矩阵，目标active。

## 10:42 删除回执只读查询

- 新增本账号回执 GET，沿用删除权限、参数化主键查询、no-store；无回执仍待核对，不当失败。未确认窗口可只查询，15秒期限覆盖头/正文，无自动POST；校验请求及全部删除范围后才成功，不自动导航。
- 247组件、36后端、10前端单元、范围eslint/tsc/build/diff通过。红测修复旧查询覆盖新关联检查提示；旧静态删除标签测试更新三态预期。真实IR1只打开/关闭预览：1600×900、768×1024、480×844无横溢，手机动作44px；视口reset、24表hash不变。实际故障与完成查询只在隔离测试中验证，未删除真实数据，无新截图。
- 删除跨刷新恢复仍未实现，下一步必须覆盖详情已不存在时的列表/公共恢复入口及账号隔离；其后文档长内容/外置输出和全矩阵。目标active。

## 10:30 检查创建账号周期隔离

- 列表/BO创建窗口按账号隔离；旧onCreated须验证账号周期及挂载，不能关闭/导航/刷新新账号；旧结果仍记原账号恢复。账号往返也不恢复旧scope权限。
- 同账号列表读失败保留身份与草稿；首次读取账号成功才启用新建，失败可重试。229组件、eslint/tsc/build/diff通过；真实三尺寸无横溢，初始账号确认后能新建，来源BO固定车辆正确，日志空、24表hash不变。实际账户未切换，异常为隔离验证，无新截图。
- 下一步检查删除跨刷新恢复与文档剩余验收，三条路径未整体完成。

## 10:21 检查创建等待上限与核对身份

- 响应头/正文均受30秒期限约束，超时保留输入并核对，不自动重发；迟到响应不能提示创建成功。确认响应比对结论、维修工与备注，兼容服务端NFKC/trim与可选空值。
- 恢复面板显示车辆、来源业务单链接及车牌筛选列表。222组件、33检查API单元、eslint/tsc/build/diff通过；真实车牌搜索命中IR1，三端窗口无横溢、可内部滚动；日志空、24表hash不变。未真实写入，异常路径为隔离测试，无新截图。
- 下一步旧账号在途创建隔离、检查删除跨刷新恢复，以及PDF长内容/外置输出和整体矩阵。

## 10:15 检查创建提交的跨刷新恢复

- 已验证账号分区、24 小时 sessionStorage；提交前暂存，刷新 pending→待核对，保留车辆及来源业务单；成功/未知状态更新，清理失败保留重试。创建服务尚无幂等保护，不自动重发。
- 214 组件、4 接口、eslint/tsc/build/diff 通过。隔离测试覆盖刷新、账号/来源过滤、迟到响应合并与清理失败；正常三尺寸新建窗口无横溢，手机继续编辑保留输入，再放弃 QA 文本，日志空、24 表 hash 不变；无真实创建故障实验及新截图。
- 待继续：创建等待上限、恢复身份提示、旧账号在途隔离、删除跨刷新恢复与文档长内容/外置输出完整验收。

## 10:04 业务保存与本机草稿清理分开反馈

- 保存/放弃显式清理，失败独立提示并重试，重试不发绩效请求；remove受限且可覆写时以无内容标记防旧草稿恢复。双重失败保持警告，不把业务成功改成失败。
- 208组件、7刷新单元、eslint/tsc/build/diff通过。正常三端窗口/放弃刷新实页无溢出或旧稿恢复，日志空、24表hash不变；异常清理分支只隔离组件验证，无新截图。
- 检查新建实页已有未保存确认，继续编辑会保留输入，勿重复报bug；待确认提交跨刷新仍是下一步。最后放弃仅QA文本未创建真实报告。

## 10:00 账号刷新后的绩效在途隔离

- 同组件currentAccountId变化创建新scope，旧保存成功/失败/finally与历史查询均不能作用新scope。两条红测复现新账号入口被旧请求禁用后修复；另测迟到历史。保留各账号草稿，不自动重发。
- 205组件、7刷新单元、eslint/tsc/build/diff通过；三端实页445px、无横溢/日志错误、24表hash不变。真实账户切换未执行，故障场景仅隔离组件验证，无新截图。
- 后续本机暂存清理失败仍须提供可见重试，不能把已成功业务保存描述为失败。其他两大路径仍未完成。

## 09:56 绩效草稿服务端目标轮次保护

- 前端原roundId→API必填正安全整数→setPerformanceDraft在FOR UPDATE锁内比对ID+版本。隔离数据库红测复现错误ID/相同版本也写入，修复后拒绝且当前字段/审计不变。旧客户端缺ID得到刷新核对提示，无猜测补值。
- 73后台、202组件、7数据刷新单元、eslint/tsc/build/diff通过。真实3220三端原值/窗口正常、日志空、24表hash不变；未实际POST绩效或运行多会话并发压测。E2E预期payload已更新但该浏览器套件未跑，无新截图。
- 仍待账号在原组件变化时的迟到回包隔离，以及其他两大路径剩余恢复/文档项。目标active。

## 09:51 绩效草稿刷新恢复与切单隔离

- 按账号/业务单sessionStorage暂存原目标及输入、未确认提交；24小时/结构校验，失败明确提示本页保留。恢复不重发，卸载后迟到结果不清待核对证据，原轮次/版本/交单变化保留草稿提示核对。
- 23文件202组件、eslint/tsc/build/diff通过；实际3220输入未提交草稿、刷新恢复，再放弃刷新回原值，三个尺寸445px高且无横溢，日志空，24表hash不变，无新截图。不是跨设备/关闭标签持久恢复。
- 待补关键边界：服务端setPerformanceDraft当前锁内只有版本而无轮次ID匹配，前端检查不能消除并发切换风险；下一轮优先修。账号在原组件内变化的迟到写回也待补。

## 09:44 绩效与维修记录等待上限

- 两条组件红测后增加绩效保存30秒、维修轮次读取15秒上限；超时表示结果未知，保留输入并核对，不自动重发。迟到响应不提示成功或清稿，JSON正文挂起也有上限。正式更正保留版本/原因/requestId，其他维修写动作未扩展。
- 191组件、7数据刷新单元、eslint/tsc/build/diff通过；数据库只读响应检查在重启就绪后成功，24表hash不变。三端实页窗口425px高、44px底部按钮、无横向溢出/日志错误，未写真实绩效；无新截图证据。
- 后续仍需跨BO、卸载迟到、刷新持久恢复；目前仅本页草稿保留。目标继续。

## 09:36 绩效响应确认契约

- 绩效保存/核对在替换状态前验证结构和业务单归属；保存核对版本、金额、轮次与正式交单更正关联。四种异常200组件测试先红后绿，14项契约含合法历史轮次更正。21文件185测试、eslint/tsc/build/diff通过。
- 强制数据库只读的check-performance-response脚本验证实际服务序列化结构，三轮历史通过；24表hash不变。三端真实窗口原值、按钮及页面宽度正常，日志空，未写真实绩效、未产生新截图。
- 仅覆盖绩效消费路径；其他维修API契约、超时、跨BO迟到和跨刷新恢复仍待处理。读取契约不是实际写入成功验收。

## 09:29 绩效表单关闭、等待与失败恢复

- 父页面保留目标、金额、原因，关闭可重开；等待时只读且在途去重，失败在页面可见，背景成功结束草稿。失败可读取最新绩效历史，关闭核对忽略迟到响应，保留原版本以避免静默重基准。
- 五条新增组件覆盖，最终167组件、eslint/build/diff通过。三尺寸真实输入/关闭/重开/放弃走通，表单425px、页面无横溢，底部动作44px。已放弃全部QA输入，重开原值-500/空原因；24表hash不变、日志空。截图失败无新目视落盘证据。
- 仍仅本页保留：超时、跨刷新、异常成功响应和跨BO迟到保存待覆盖，真实绩效未提交。下一轮继续隔离故障矩阵与接口契约，不把正常表单DOM验收当成真实财务写入成功证据。

## 09:22 轮次阻断入口与只读复核

- 修复已交单绩效误写尚未计入；增加交单历史、正式单据、本单检查入口与重新检查。复核失败可重试，关闭忽略迟到结果，轮次变化不更换删除对象。保持既有正式单据留存规则，未执行真实删除或绩效修改。
- 162组件、范围eslint、生产build/diff通过，24表hash不变。真实手机/iPad/桌面按钮44px、页面无横溢；复核、历史、单据导航走通，日志空。截图不稳定，没有新落盘目视证据。
- 只读复核超时/跨刷新恢复、绩效调整关闭后结果处理及删除持久恢复继续待办；这不是全流程完成声明。

## 09:10 PDF 长描述续页与预览实页回归

- 新有效标签恢复浏览器访问，未绕过策略。1600×900/768×1024/480×844实际canvas和三语言文件名核对，放大、切语言清旧文件、隐藏旧HTML打印、44px按钮通过；页面宽度不溢出、日志空。截图工具仍失败，实页视觉不记通过。
- v2长描述全宽展开，续页带项目序号/名称引用，JOB与收费只一次；正常闭尾汇总/责任/签字保持一起分页。4条真实PDF红测后修复，14项模型/PDF/打印、158组件、范围eslint/build/diff通过。
- QA/formal-pdf-v2六样本普通三语各1页，长zh3/en3/bi4；所有长页目视，签字块调整后final-*重渲，无孤立签字页。未知价仍待补，长文末尾/A4/坐标验证；中文行首标点偶见、超长责任块待覆盖。
- 部署PID79655候选apps/web；部署后IR1真实生成canvas回归、24表hash不变。尚无实页最终下载外置落盘/纸张证据，隔离长页导航与资源失败重试尚待做；后续优先轮次、绩效调整和删除恢复移动端矩阵。目标active。

## 09:02 检查 PDF 实际预览与同文件打印

- 生成后的下载Blob字节用于PdfCanvasPreview和printPdfBytes；原HTML读区及旧打印入口隐藏，语言/来源变化清旧文件。预览失败保留下载、可原地重载；打印失败可下载/重试。
- 新可选AbortSignal阻止语言切换后旧打印准备迟到启动，完成准备后释放副本；不宣称已取消PDF.js CPU工作。20文件158组件、4打印单元、tsc/eslint/diff通过；源代码build通过且部署PID78786，24表hash不变。
- 实页集成尚未验收：重启后错误页 data: URL 被浏览器策略挡住，未绕过。没有新增截图/最终下载/实体打印证据。待验三尺寸canvas、缩放/翻页、原HTML隐藏、隔离失败恢复；最终落盘仍需外置安全路径。

## 08:54 检查 PDF 生成与下载入口

- 新独立formal模型/渲染器/生成适配器/组件，HTML与PDF共用金额完整性规则。三语言、真logo、JOB、分类合计、未知价/译文保留、长行分页表头，原生打印之外可生成下载文件。生成中/取消/重试、语言与来源切换不交付旧文件，资源15秒超时，页内blob清理。
- 155组件、6模型/PDF测试、eslint/build/diff通过。6外置样本PDF：普通三版各1页，长zh4/en6/bi10，A4与全文末尾/文本坐标验证；真实渲染发现签字预留过大后修复并新增回归，最新normal-bilingual-final.png核验签字同页。
- 实页3尺寸生成ZH/EN/BI链接，44px，无页面横溢/运行错误。下载最终保存未点击（当前未知可指定外置落盘的浏览器能力）；PDF样本外置落盘独立验证，不能等同实页下载完成。截图工具两次失败，无新增87+截图，入口视觉验收待补。24表hash不变，PID78380。
- 下一步：PDF实际预览与HTML布局一致性、安全下载落盘验证、入口视觉/故障隔离实页，长描述续页身份与版面利用。目标active。

## 08:41 正式检查报告分类合计

- 正式A4报告按工时/配件材料/其他费用分组，独立表头、分类折后合计；不显示空类别，分类待确认保持。三语言标签，JOB，金额不换行。缺价类及整单仍待补，明确零元保留，整单优惠不分摊重复扣。
- 2红测后修复及1空分类测试，全组件152、eslint/build/diff通过。3220实页IR1工时15000/配件800/总15800已核对。隔离三尺寸84–86目视，手机内部横滑到金额列，页面无横溢、日志空。24表hash未变，PID77328。
- 这是HTML单据内容验收，不是最终PDF/纸张验收。下载输出与长页分页仍待做，目标active。

## 08:34 检查报告打印隔离与启动失败恢复

- 独立body直系sheet快照，全文/语言保留、zoom1，A4每页12mm边距/186mm内容区；清理旧常驻全局print visibility。beforeprint/afterprint生命周期，3秒无响应/异常可再试，副本样式和标题清理、原预览不动，44px动作。
- 3条组件先红，149组件及范围eslint/build/diff最终通过。82桌面模拟无响应、83手机模拟异常目视；平板模拟启动确认body副本全文一致/zoom1，取消清理、原zoom1.5；三尺寸无横溢，日志空，隔离服务已停。
- 真实3220 PID76969点击打印未发出beforeprint，超时提示生效、按钮恢复、无遗留副本。**没有最终PDF或实体纸张，不能宣布打印输出通过**。后续补当前formal报告的下载输出路线和工时/配件分类统计；旧ir-pdf生成器不兼容现合同，不能盲目复用。
- 24表hash未变，无真实业务写入，目标active。

## 08:28 客户回复连续输入与错误反馈

- 3条红测复现迟到成功清新稿、旧成功notice残留、同帧双提交。提交快照对照当前表单，后续输入保留未提交；清旧notice、ref防重、按钮等待状态/44px。原正常成功仍清原稿，不自动提交后改内容。
- 第一次全组件144/145揭示历史GET清空新操作错误，确定性迟到GET测试再红后分离loadError，读取失败可重新读取，不抹操作错误。最终146/146、范围eslint、生产build/diff通过。
- 隔离生产组件/API适配器仅内存记录，79平板失败、80手机等待、81桌面成功保留新稿已目视；三尺寸无页面横溢，等待切标签返回原稿仍在，日志空。真实IR1只读跟进回归，无真实回复或客户通知。24表hash未变，PID76502。
- 当前仅页内连续操作恢复，不是跨刷新持久化或通信幂等保证。检查报告打印输出/祖先裁切仍未验收；目标active。

## 08:21 检查报告独立A4阅读区

- 固定210mm纸张，适宽/100%/150%三档，区域内纵横滑动；金额nowrap，页面不横溢。ResizeObserver处理适宽随容器变化，手动缩放保持。print CSS恢复zoom1/A4并解除新阅读区裁切，原window.print动作不变。
- 新增两条实际组件回归（首条先红），全组件142、范围eslint、生产build/diff通过。隔离真实组件截图76手机150%、77平板适宽、78桌面100%均目视。实页480×844测得sheet793.6875/reader408/scroll794/page480，金额nowrap，三档操作可用，错误日志空。服务已停、视口reset。
- 部署PID76017，24表hash不变。只读实页/内存样本，未改财务、绩效、报价事实。未发实体打印任务，亦未验收检查报告最终PDF；祖先overflow对HTML打印的影响仍待查，不能以CSS或组件测试替代打印验收。

## 08:14 缺价不冒充零元报价

- 四条真实组件红测后修显示资格，不动数字计算或财务事实：空/部分缺价报价待补，明确不报价单独显示；真正0、明确行总价、已知分类合计保留。查看/编辑/核对/正式报告共用判断，英文Pending。新增五测试含三状态参数化，中英报告断言；全组件140通过，范围eslint及最终build/diff通过。中途仅测试exact选项类型错误已修。
- quote-status隔离生产组件/适配器，只读内存样本，其余API405。73桌面、74平板0元、75手机英文报告已目视。74/75最初viewport截图为空，改fullPage覆盖；最终DOM宽1600/768/480，无页面横溢，但75手机金额换行、表格窄已列下一修复，不能宣称可读性通过。
- PID75585已部署；24表hash不变，历史待补0。真实IR1只读加载正常，未保存/调用AI/发通知。QA已停止，视口恢复。继续报告手机阅读、recordReply恢复提示、PDF签字区及其他原有待办，目标active。

## 08:04 AI迟到结果与撤销恢复

- 三条真实详情组件红测：AI返回覆盖后改人工稿；撤销整理回到旧数据库内容；停止入口不存在。现用请求序号/AbortController；有后续编辑则保留当前稿并提供完整AI核对，应用只暂存，撤销恢复应用前人工稿。停止后旧请求不能改稿或解除新请求等待；失败可重试且指令保留。
- 全组件135项、API/报价48项单元、范围eslint/最终build/diff通过。最后按钮busy禁用与执行guard一致后再次135项及构建通过。PID74941及cwd核实；真实IR1只读加载正常，24表hash未变。
- 隔离实际生产组件+AI适配器，全部API内存模拟、业务写入405。70桌面等待、71平板核对、72手机核对均已目视，1600×900/768×1024/480×844，手机动作44px且无横溢；应用→撤销、停止→迟到无效、重试失败保留稿均实际操作通过。服务停止，尺寸复原，无付费AI调用。
- 暂存/撤销仅当前页；取消客户端请求不证明供应商计算/计费停止。语义报价质量未用付费模型验收。pending编辑器金额显示、recordReply提示及其他原有待办继续，目标active。

## 07:57 保存期间连续编辑保全

- 两条真实详情组件测试先复现：保存返回旧内容覆盖等待期间新输入；下次失败同时残留上次成功。增加事件时草稿ref，成功只更新提交快照对应版本，后续编辑保留并显式提示未保存，下次人工保存使用已确认expectedVersion。保存开始清除旧notice并防重复在途提交。
- 全组件131项、范围eslint/生产build/diff通过。最初误用不存在的配置文件启动失败，改正确workspace配置后获得2红测；未以启动失败当作功能复现。部署PID74358及候选cwd核实，真实IR1编辑入口回归正常、日志空，24表hash不变。
- 隔离save-continuity使用生产详情与API适配器，第一次延迟成功、第二次409失败、GET返回模拟已保存快照，全部API本机内存不接库。67/68/69三截图分别1600×900/768×1024/480×844，原稿保留、失败提示无旧成功、核对能区分后续编辑与提交内容；手机动作44px、sw480，全部目视，服务器已停。
- 未验证AI迟到结果保护；报价编辑器pending显示0与查看页待补不一致已发现待修。未扩大到真实保存或其他业务模块全面验收。

## 07:50 关闭等待后的检查创建恢复

- 列表/业务单两入口共用父页面恢复队列，逐次快照记录迟到成功或失败。失败恢复完整原稿，当前另一份草稿不会被覆盖；两次逆序完成分别归属。没有自动重发POST，成功只提供结果入口。恢复只限当前页面，离开/刷新前处理的提示可见。
- 真实组件红测两条后修复，全组件129项、范围eslint/build/diff通过。构建后仅追加一条逆序测试；PID73784已部署。真实3220列表和BO1来源表单可打开取消，车辆/班组正常，日志空。24表hash未变，未真实提交。
- 隔离3228中间件模拟等待和500，实际生产列表/表单/API，无数据库。最终64桌面1600×900、65平板768×1024、66手机480×844已目视，恢复/核对动作44px，手机sw480，恢复后原车辆/班组/结论/备注完整。首次截图视口针对错误标签，发现后重测并覆盖；倍像素截图外围空白不是DOM横溢。服务已停止。
- 未完成创建跨页面持久恢复及幂等键；保存中编辑和旧成功提示仍待查，不能将本轮等同整个检查工作流完成。

## 07:39 报告保存与核对响应校验

- 原损坏200返回空对象进入setDetail，实际组件抛workspace.versionNo异常并清空页面。API15失败和真实组件失败先验证，再新增详情响应schema校验；保存需报告身份相符、记录版本+1、来源相符。读取核对结果也校验结构，不能用坏响应替换草稿。
- 保存网络/500不确定状态提示核对而不自动重试；明确409错误保留。实际API+组件模拟覆盖草稿保留、核对读取失败可重试、采用系统确认版本不再PATCH。全组件126项、API33项、范围eslint/build通过。最终绿色测试无未处理异常；最初版本标识断言过宽已收紧。
- 3220 PID73351真实IR1读取与核对正常，日志空，未触发真实保存。外置save-response中间件只模拟内存结果，无真实库连接；截图61/62/63覆盖桌面/平板/手机，手机两动作44px且位于屏幕内，sw480；均目视。截图失败仅同一页重试，Vite future native loader警告属于QA配置，服务已停止。
- 24表hash未变。此轮未改任何财务/绩效规则、PDF或真实数据；未完成关闭/卸载后的创建失败草稿恢复、保存中继续编辑竞态、旧成功提示残留检查及跨刷新删除恢复。

## 07:31 检查创建异常响应恢复

- 实际API适配器异常201退回空对象导致假成功/undefined详情链接，API九项及实际组件先红后绿。成功需返回匹配车辆/来源业务单/班组的正整数ID与非空编号；不确定结果不自动重试，当前表单保留，另开列表核对。明确400信息保留，独立检查null来源合法。
- 新增实际业务单+创建表单后台完成覆盖，关闭等待窗口后刷新关联列表而不强制跳走；生产BO回调本已如此，本轮未更改其行为。全组件125项、API单元17项、范围eslint及最终生产build通过。初次build的测试fixture类型问题已修，根目录lint忽略文件的问题通过apps/web范围执行纠正。
- 隔离3228直接引用生产表单与API适配器，201截断响应只由本机中间件模拟，其他API关闭，不连接业务库。截图58桌面1600×900、59平板768×1024、60/60b手机480×844均目视，手机44px核对链接及底部动作可通过Tab滚动访问，无页面横溢。隔离配置最初缺@formal别名和浏览器process公开变量，均补齐后验收，不是3220生产故障；验收服务已关闭。
- 3220 PID72795实页业务单重新加载、检查创建入口及关闭通过，日志空、恢复原生2560尺寸。只读24表hash不变，未真实创建/删除/财务绩效写入。创建未幂等化且卸载后失败草稿恢复仍待做；报告保存响应校验、PDF签字区预留、轮次恢复等未完成。

## 07:18 单据信息完整性增量

- infoGrid两行截断已修；按实际行数撑高，超一页按可用高度分片，续页标签，后续行顺延。中文/英文/维修工联六条测试先真实失败missing identity line 2，再全PDF46项通过，覆盖6/90行保全、页脚边界、下行不重叠及双联一致。
- 渲染器v20-complete-identity，普通短信息高度保留；隔离identity-wrapped-zh/en均2页，identity-long-zh/en压力样本均6页。16张PNG全部目视，无丢字或重叠；自然英文客户及车型各3行可见末尾。样本不是业务记录，历史PDF未重生成。
- 全组件123项、范围eslint、生产build、diff通过，服务PID72185，24表hash未变。真实3220仅回归旧单据读取，不宣称真实库长名字的新增单据已经人工生成验收。此前三尺寸预览证据仍属07:12，本轮没有改预览组件。
- 保留待办：压力样本末页仅签字，检查签字区预留高度；BO来源后台创建专测与创建API响应、绩效轮次恢复及删除跨刷新待续。以下旧检查点中的infoGrid待办已由本轮覆盖。

## 07:12 长双联导航增量

- 预览显式copyLabels按实际总页数分配联别页段，续页标题保留办公室/客户联；6/10页可跳联并直接选择末页。页数不能整除联数时不猜边界，显示核对提示和普通逐页导航。旧单页模式不变。
- 修customer_copy v18/v19渲染器名称不含-paired导致误判旧单联：识别v16起配对契约，v15仍旧单联。版本识别通过组件mock响应验收，未在真实库生成v18/v19文件。
- 先失败再修复；全组件17文件123项通过，生产build/范围eslint/diff通过，PID71525。24表哈希未变。此前PDF40项结果仍属07:02，本轮未改PDF生成器。
- 外置3228隔离入口引用实际生产组件与PDF.js，英文6页/中文10页，1600×900、768×1024的150%放大、480×844的末页导航可操作；截图55/56/57目视，手机组按钮44px、页面sw480。隔离开发HMR重复createRoot警告补dispose/unmount，非3220错误；服务器验收后关闭。
- 3220真实CUS V20回归：2个canvas、办公室/客户联标题、客户页跳转正常，原生2560宽无横向溢出，日志空。未触发真实生成/打印/通知/财务写入。
- 下一项：infoGrid slice(0,2)长身份信息截断；其他未完成项不因本轮通过而视为完成。

## 07:02 长单据分页增量

- PDF说明/备注/问题/签字条款超过一页时越过页脚已修，8项中英长文本逐行保全/两联一致/末页签字回归；中文闭标点独占行亦修。PDF全40项通过，普通双联仍2页；超长样本允许每联续页。
- render-pending-quote.ts --long输出中文10页、英文6页，16页PNG均目视通过，没有改变真实联。长备注/条款/问题主要为自动PDF文本坐标及内容验证，未逐一另出视觉样本。
- 三尺寸已有双联跳页、语言切换、150%缩放/适合宽度实页验收，截图53/54。仅预览内部横滚，页面无横溢、工具可达、日志空。未触发实体打印。
- 全组件首轮1项测试竞态，修正等待重试失败后按钮可点再取消，连续两轮118项通过；eslint/build/diff通过。24表hash不变。
- 下一轮首要：长单预览联别名称丢失（pageLabels两条但PDF多页）；其次infoGrid两行截断的超长身份信息。以上未完成，不以分页测试宣称全部单据验收完成。

## 证据口径

- **实页**：实际 3220 已登录页面，普通读取、打开、取消、未保存输入和尺寸检查。
- **组件**：真实 React 组件与模拟 API，覆盖失败、并发及迟到响应，不等同真实网络断连。
- **集成**：隔离数据库/真实业务服务或 PDF 渲染器，不向真实客户写业务事实。
- **待查**：尚无充分证据；不能因为其他测试通过而视作验收完成。

## 业务单明细（09-05 14:28 合并增量，旧待办不再重复执行）

| 操作 / 规则 | 当前证据 | 缺口或边界 |
| --- | --- | --- |
| 工时、配件分组，名称与描述分开，类别净额和总额 | 实页桌面/平板/手机；business-order-workspace 组件；business-order-calculation 集成 | 真实多币种/超长项目名的极端宽度尚未统一复核 |
| 工时 JOB，整数 QTY，旧小数不擅自取整 | 实页输入及错误定位；组件、schema/AI 单元 | 历史非零小数保持并提醒人工，不做批量数据修正 |
| 收费失败保留、并发核对后再次保存 | business-charge-edit-recovery 组件；实页只读核对/取消 | 正式保存及冲突由模拟 API 测，不提交真实收费 |
| AI 等待、停止、重试，人工内容优先，跨单隔离 | 组件覆盖迟到结果/人工更改/切单；实页输入与取消 | 未调用付费 AI；未全面验收 AI 语义报价质量 |
| 待报价不是免费 0 元 | AI/parser/schema/服务/PDF 回归；实页勾选再取消；0051 迁移守恒 | 历史 0 元不自行推断为待报价 |
| 右栏维修与收付款连续排列，历史展开 | 04:36 实页三尺寸，无横向溢出 | 最终全路径统一尺寸截图尚未补齐 |
| 售后轮次绩效独立、可调整、撤销留痕 | formal-handoff-service 隔离集成；09:29–10:04 已补三尺寸调整、关闭重开、刷新草稿、账号隔离、等待期限和保存响应核对 | 不改真实绩效；异常保存由隔离测试覆盖，不等同真实付款/绩效写入验收 |
| 无效轮次删除可查看依赖并处理 | 14:24 实页复核/交单历史/正式单据链接走通；391×844、768×1024、1600×900无横溢；截图88已目视。关联读取15秒期限已存在 | 当前R3同时有有效交单与正式单据，取消交单不解除单据阻断；没有已批准的单据作废规则，不能宣称任意无效轮次均能删除。不执行真实删除 |
| 整单折扣 | 前端草稿保留有测试，后端明确强制 0 | **规则资料冲突**。不能以组件测试宣布全链路支持；先查获准决策，暂不修改金额算法 |
| 维修轮次删除提交恢复 | 14:29提交期限；14:43原请求24小时恢复、账号隔离、异常回执、暂存失败、读取最新历史隔离回归 | 不自动重发；未执行真实删除；专用只读回执查询尚未接入，历史核对不等同结果回执 |

## 单据与打印

| 操作 / 规则 | 当前证据 | 缺口或边界 |
| --- | --- | --- |
| 费用确认单办公室联+客户联连续两页、一起生成 | 普通中英PDF各2页已目视；07:02–07:18补长列表6/10页联别、续页、长身份不截断；14:19真实现有V21/R1连续2页 | 长单自然分页可超过2页，不压缩或截断内容；无批量重生成历史单据 |
| 两联费用与责任一致，不夹带收付款事实；客户中英分开 | document-print-boundaries 组件；business-order-document-pdf 集成；隔离中英 PDF | 未批量重生成历史单据 |
| 维修工联显示本单本轮绩效 | 实际组件签名 −500；PDF 渲染测试 | 真实业务当前轮只读，不调整金额造样本 |
| 缩放、连续预览、旧版本/旧文件 | 组件与长PDF样本；14:19当前实页1600×900、768×1024跳联，391×844的150%只在阅读区横滚，外页不横溢 | 长QA样本未写进真实系统；实际下载外置落盘仍未验证 |
| 选新文件立即停止打印旧文件，失败重试/修复有入口 | document-selection、render-error、request-retry 组件 | 网络中断均模拟；未损坏真实 PDF 制造故障 |
| 全部页面渲染后才打印；失败可下载相同 PDF | pdf-print-surface 组件；ir-pdf-print 3 项真实函数/注入宿主测试 | 用户之前确认系统打印可用；本轮没有发实体打印任务，不能声称验收纸张输出 |
| 过期收费版本提醒 | 实页保留 V20/R7 与当前 V21 差异提示 | 旧单据不是错误数据，不自动重写 |

## 检查结果

| 操作 / 规则 | 当前证据 | 缺口或边界 |
| --- | --- | --- |
| 独立车辆检查、可关联业务单，检查不改正式收费/派单 | inspection-report-service 隔离集成；实页两个创建入口 | 不创建真实 QA 记录 |
| 列表搜索、返回保留筛选、无效页码恢复、失败重试 | 实页；inspection-list-access 组件 | 多页不足，真实多页由组件样本覆盖；系统中文候选窗未手动测试 |
| 新建加载/搜索失败重试，成功空结果才新建车辆 | 组件模拟失败/迟到；实页正常/空结果 | 真实网络错误未人为制造 |
| 脏草稿关闭确认、保存中继续等待/明确退出、嵌套车辆焦点 | 实页四尺寸含 844×320；create-continuity/focus-recovery 组件 | 已退出请求不代表撤销服务器提交 |
| 保存晚到不关后来窗口；已保存但跳转失败不允许重复创建 | create-continuity/list-access及BO组件；07:31后补来源后台回调、异常响应；10:15–10:30覆盖24h刷新恢复、30秒期限及账号周期隔离 | 竞态与导航异常为隔离验证；非幂等创建未知结果需人工核对，不自动重发 |
| 英文待补入口、JOB、版本核对/并发失败恢复、状态更正弹窗 | 实页三/四尺寸，核对末尾可达；inspection-detail-access 组件。06:45新增后台保存可退出/重开同请求/失败保留状态原因/成功通知两条回归；异常JSON/空响应/错报告身份拒绝假成功 | 待返回请求可查看报告，仍锁同页相关写操作防重复。真实长等待、失败仅隔离测试；通信API尚无前端幂等requestId或自动提交结果核对，未知结果需人工核对历史，不自动重发 |
| 客户跟进回复成功不假报失败 | 组件回归；真实列表/详情只读 | 未发送短信/邮件/WhatsApp，未提交真实回复 |
| 删除检查结果错误恢复闭环 | 三尺寸预检/重查/客户跟进；06:50–14:09补执行退出、原请求重试、成功导航异常、15/30秒期限、回执只读查询、账号隔离与24h公共恢复入口 | 报告1有3条沟通记录，现行策略禁止删除；查看入口不等于作废/解除依赖闭环。未执行真实删除；故障/恢复用组件和隔离服务验证 |
| 检查PDF三语、分类合计、长责任分页、标点不孤行 | 08:41–14:09增量；真实生成器中文/英文3页、对照5页，共11页已目视；14:19三端语言控件命中修复并实页回归 | 下载落盘/实体打印未执行，不把浏览器生成预览当成打印机验收 |

## 历史验证结果（06:27–06:30快照；最新结果见顶部检查点）

- 06:27：全组件 16 文件 108 项通过；范围 ESLint、生产构建通过。
- 06:28：5 个核心后端/PDF/交单测试文件共 73 项通过，maxWorkers=2。
- 06:30：ir-pdf-print 3 项通过；git diff --check 通过。
- 实页新建草稿放弃、业务单来源空窗取消正常，日志 error/warn 为空；原生 2560×1440 无页面横向溢出。
- 24 张真实业务表与迁移前快照哈希一致，historicalPendingCount=0。没有写入真实收费、收付款、绩效、检查、车辆或客户通知。

## 06:50 增量验收（优先于下方旧顺序）

- 共享删除组件8项、删除API6项、后端隔离14项通过；全组件17文件118项通过，ESLint及生产构建通过，部署PID70240。
- 执行中可退出/重开原请求、失败同payload重试、成功与导航失败分开、异常身份/范围响应拒绝假成功。以上执行情景为组件模拟及后端隔离测试，不是真实记录删除。
- 实页1600×900、768×1024、480×844预检/退出通过，无横溢，手机按钮44px，截图52；日志空、24表哈希未变。
- 删除请求刷新/卸载后的持久恢复仍未做；客户沟通记录现行策略仍阻断，不宣称作废路线完成。
- 下一轮先做超长双联PDF/手机平板缩放与末页动作，再补业务单来源创建后台回调专测和创建API异常响应校验。

## 已被后续增量覆盖的历史顺序（06:32存档，不作当前待办）

1. 正式删除执行中的无期限请求退出/提交结果不确定；已完成删除预检及状态更正退出恢复，不重复修。沟通事实无法作废的现行限制需查已批准业务规则和服务能力，不能凭按钮入口宣称闭环。
2. 业务单来源新建检查的背景完成回归，确保不跳走、不关闭另一份草稿。
3. 单据超长项目、连续两联、手机/平板缩放与末页动作的统一实页/隔离 PDF 验收。
4. 绩效调整、无效维修轮次依赖提示的只读操作路线复查。

目标继续 active；本矩阵不是待用户答复清单，以上安全检查可自主推进。
