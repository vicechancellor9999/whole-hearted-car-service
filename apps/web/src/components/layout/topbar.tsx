"use client";

/**
 * The fixed 64px top bar has been removed per collaboration task.
 * Theme toggle and notifications are now hosted inside the dashboard header.
 */
export function TopBar() {
  return null;
}
